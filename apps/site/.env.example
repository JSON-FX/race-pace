NEXT_PUBLIC_SUPABASE_URL=https://whaqarofxdlzxrelbcrq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Public provider keys. The matching secrets stay in Supabase Auth or Edge
# Function secrets and must never use a NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=

# Optional. Google Cloud → Maps Embed API key, restricted by HTTP referrer to
# your own domains. When set, the event page's course map uses the official,
# documented Maps Embed API. When unset it falls back to the keyless
# maps.google.com/?output=embed form, which works but is not a supported API —
# set this before public launch. No code change is needed either way.
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
