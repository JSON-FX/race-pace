# Web and admin launch progress

Updated: 2026-09-20. Overall: MVP RELEASED; PILOT ORGANIZER SETUP PENDING.
Scope: runner website and admin only. The first release is a controlled pilot with one organizer and PayMongo. Production is open with a clean event catalog; the first organizer and event will be real data.

Use the release-critical table below as the launch decision. Keep the original 20-item checklist and detailed evidence as supporting records. Update affected rows after every completed action, failed verification, or blocker. Do not mark a row complete on submission alone. The dated hosted checkpoint is historical supporting evidence; these tables take precedence for current status.

Production access check, 2026-09-18: PR #41 merged to main at `768e245` and both production Vercel deployments reached READY. Unauthenticated requests to the public site and admin still land on Coming Soon; both unique deployment URLs require Vercel Authentication and expose the sign-in screens only after that protection. The seeded `admin@racepace.test` super-admin account accepted its published repository password in production, so the role was moved to the confirmed existing `json.alanano@gmail.com` account, the seeded role was removed, and the seeded user was banned through Supabase Auth Admin. A repeat password sign-in was rejected. Production main CI run `35358615806` passed. The staging PayMongo test webhook was briefly paused after confirming no pending staging registrations or refunds, then restored while waiting for named-admin sign-in. The separate production test webhook is configured and signed-request verified, but paused until the production checkout window. Enable it and pause staging for that window to avoid cross-environment callback retries, then restore it immediately afterward.

Production backend cutover, 2026-09-18: deployed 18 current Edge Functions to production, excluding local-only fake checkout. Confirmed JWT verification flags. Configured a distinct PayMongo sandbox webhook and secret, copied the verified test API key, and scheduled the expiry worker every five minutes using a new Vault/Edge bearer secret. A worker response returned HTTP 200 with no candidates. Public production routes remain on Coming Soon.

Protected production smoke, 2026-09-18: Google OAuth reached the named super-admin session on the exact protected admin deployment. A 0% `[TEST] Production Sandbox Organizer` was created through the admin form and read back with `commission_rate=0`, `commission_flat_cents=0` and an admin role for the existing named account. Its generated manual invite link exposed an outdated admin host; `ADMIN_APP_URL` was corrected to `https://admin.racepace.com.ph`, with fresh-link delivery verification still pending. The protected runner Google sign-in succeeded but `/races` crashed because the production root layout omitted `QueryClientProvider`. The admin switcher also showed the new organization while Events still queried the old one because role resolution preferred an existing admin row over the selected organization. Both release blockers have narrow fixes in PR #44, pending CI, staging merge and production browser verification. No production purchase or refund has been attempted. The sandbox organizer is prelaunch test data for scoped cleanup before launch.

Staging scope regression, 2026-09-18: PR #44 passed both required validation jobs and merged to staging at `7f7285c`. Vercel reported both staging apps Ready. The staging super admin signed in with Google, created `[TEST] Staging Scope QA Organizer` at 0% commission with the existing account, and switched between it and the original staging organizer. The new organization's Events page showed 0 events and 0 registrations; switching back restored the original 7 events and 7 registrations. The generated staging manual invite link used the correct staging-admin host. The synthetic scope organizer remains in staging as QA data.

Protected production retest, 2026-09-19: PR #45 passed both pull-request validation jobs and merged to `main` at `fab2bab`. Both exact-commit production Vercel deployments reached Ready; the exact-merge main CI run `35365591185` passed. The production Supabase redirect allowlist includes only their two exact callback URLs. Google sign-in on the protected runner URL loaded My Races with the existing paid ticket; the `QueryClientProvider` crash is resolved. Google sign-in on the protected admin URL loaded `[TEST] Production Sandbox Organizer` with 0 events and 0 registrations, resolving the selected-organization scope error. Public site and admin routes still redirect to Coming Soon. A synthetic, unpublished ₱100 event with three slots was created under the 0% organizer; it has no registrations. The existing named account created one unclaimed synthetic participant Passport, and the runner profile reported its required details complete. No production payment or refund has been attempted.

Production sandbox registration, 2026-09-19: With explicit approval, the test-only organizer waiver was published and attached to `[TEST] Production Sandbox Checkout QA Race`. The event opened at ₱100 with three slots. The protected runner checkout selected the complete synthetic `Production QA Runner` Passport, shirt size M and the published waiver. With separate approval, one reservation was submitted. Registration `62b54f16-0645-401b-8b90-165f07d3c46d` was read back as pending with a pending ₱100 payment and no ticket. The checkout displayed exactly ₱100 due and ₱0 added fees. The sandbox payment handoff did not start in this session, so payment, QR, refund and accounting remain unverified. The production sandbox webhook was disabled and the staging webhook re-enabled after this paused handoff; provider readback confirmed both states. Do not count the reservation as a paid smoke test.

Production paid smoke, 2026-09-19: The pending test registration was paid through PayMongo test-mode GCash after re-enabling the production sandbox webhook and pausing staging. PayMongo GET independently returned one non-livemode paid capture of ₱100 with a ₱2.50 actual fee and ₱97.50 provider net. The production ledger matches: one paid payment, one settled capture, one occupied slot and one signed QR ticket for `Production QA Runner`; zero Race Pace commission gives ₱97.50 organizer net. The protected ticket visibly renders its QR. The user confirmed receiving the test email. Admin Payments and Settlement display ₱100 gross, ₱0 platform fee, ₱2.50 processing and ₱97.50 net. The PayMongo return landed on the public Coming Soon gate because the protected deployment uses the production public callback URL; the ticket was reached through the protected deployment. Payments and Settlement export clicks produced browser download events, but the in-app browser did not expose inspectable CSV bytes. Refund preview shows ₱97.50 returned and ₱2.50 retained; final sandbox refund awaits the account owner's browser action.

Production refund smoke, 2026-09-19: The account owner confirmed the final ₱97.50 PayMongo sandbox refund in protected admin. An independent provider GET reported `succeeded`, ₱97.50, the expected payment ID and `livemode=false`. The production `refund_requests` row is succeeded with zero retained organizer net. Registration and payment became refunded; category occupancy returned from one to zero. The protected ticket now says “Registration refunded” and no longer shows its QR. Admin Registrations shows one refunded entry, ₱97.50 refunds and ₱0 retained gross. Admin Payments summary shows ₱0 retained gross and net; Settlement shows ₱100 original gross − ₱2.50 processing − ₱97.50 refund = ₱0 payable. At the time of this smoke, a display discrepancy remained: the individual Payments table row still labeled its historical ₱97.50 pre-refund net simply “Net” while the summary shows current ₱0. A narrow UI fix in PR #47 shows current ₱0 on full-refund rows and disables sorting by the historical ledger value. It passed 881 admin tests, typecheck and exact-merge staging CI, and merged to staging at `b6741c9`. The Git-created staging deployment remained Initializing and was canceled. An exact-commit prebuilt admin deployment `dpl_ATRrbRXxjdQRECVTmXFhwyaoRPcD` reached Ready and the staging-admin domain resolves to it. In the live Payments page, the fully refunded row shows ₱0 and partial-refund rows retain their current net. The fix was promoted to `main` in PR #49 at `c1735cb`. Exact-main CI run `35400498651` passed, and production site/admin deployments `dpl_7uRvjyQUqN7DbHDP6txbzagoVegM` and `dpl_4cBsPg1765h6rgxeWVfULMBM9uDY` reached Ready on their custom domains. Public routes still redirect to Coming Soon. The production Payments row has not been visually rechecked behind the protected deployment. CSV download events fired before refund, but their bytes were not inspectable. Production sandbox webhook was disabled and staging re-enabled; provider readback confirmed both. Public production remains Coming Soon.

MVP launch scope decision, 2026-09-19: The owner accepted the proven organizer → event → runner registration → PayMongo payment → QR ticket → admin refund and accounting flow as the launch baseline. Advanced recovery automation and nonessential polish move to post-MVP tickets. Issue #51 tracks the missing operator workflow for a distinct second PayMongo capture. Existing code durably records that anomaly, prevents a second ticket or slot, alerts platform staff and blocks payout, so the pilot can escalate it manually without corrupting registration or organizer accounting. The unfinished implementation was removed; only the investigation artifact remains.

Production preflight refresh, 2026-09-19: Both hosted Supabase projects report `ACTIVE_HEALTHY`. Production has seven completed physical backups; the latest completed at 2026-09-18 19:25 UTC. Point-in-time recovery remains disabled, and database backups do not cover Storage object bytes. Production Vercel environment inventories include the required public Supabase URL/key for both apps and the public site URL for the runner app. Production Supabase Edge secrets include PayMongo, webhook, Resend, ticket, worker and application URL entries. Secret values were not exposed. The PayMongo production secret remains the previously verified sandbox credential until the live key and live webhook are installed for launch.

Production cleanup and Storage recovery, 2026-09-19: A restricted snapshot downloaded all 48 production Storage objects and the pre-cleanup data inventory. The archive contains 14,684,658 bytes and has SHA-256 `c6305ef2ece202d0ab52cbd847f75576864c32ade9b34ff58f6c476c83ccd29e`. A clean extraction verified every object against its manifest checksum and byte length. The scoped cleanup first removed the explicit `[TEST]` organizer, two events whose descriptions identified them as non-real QA races, their PayMongo sandbox ledger/refund/capture records, five related notifications, two synthetic Passports and the banned seeded `.test` Auth account. At the owner's direction, the remaining organization, event and three categories were then removed so production can start without organization data. All 34 event images and both organization images were removed after backup. Readback now shows four named accounts, zero organizers, zero events, zero registrations, zero payments and zero notifications. The named accounts, platform super-admin role and 12 profile images were preserved. See `production-cleanup-2026-09-19.md`.

Pilot acceptance preparation, 2026-09-19: The platform owner accepted the proven MVP journey and its stated limitations. The technical acceptance package is recorded in `pilot-acceptance.md`. The first real organizer and event will be created through live onboarding. Final organizer sign-off still requires confirmation of event content, prices, staff access, pilot limitations and the decision to open registration. No external acceptance was fabricated or sent.

Production opening and QR Ph rollout, 2026-09-20: PR #54 merged to `main` at `400da69`. The temporary Coming Soon redirects were removed from both production applications while the staging middleware retained its crawler-blocking headers. The production runner home returns HTTP 200 with the empty-catalog message, and the production admin events route redirects unauthenticated users to sign-in. Migration `20260919164126` is recorded in production and enables the current QR Ph processor rate, expands the group-payment method constraint, and replaces the group preparation procedure with explicit `service_role` execution only. The payment-session and group payment functions now accept PayMongo's documented `qrph` method through explicit server allowlists. The payment webhook remains signature protected; an unsigned production request returned HTTP 401 `invalid_signature`. Exact-release validation passed 410 runner tests, 881 admin tests, 704 backend/shared tests, both typechecks, both production builds, GitHub CI and both Vercel previews. No live QR Ph charge was made at the owner's request. The next action is to create the first real organizer and event, then run the owner-led QR Ph checkout.

Branding cleanup, 2026-09-20: Removed the former organizer's name, sample identifiers, event-image URLs, dedicated incident document and two branded login photographs from the current repository. Neutral TrailNorth fixtures now cover organization behavior without suggesting a live partner. The owner selected an original Race Pace illustration showing road runners approaching fictional tropical mountains; the optimized 1536×1024 WebP is shared by the runner split-screen login and admin banner. Both layouts were inspected in production builds. Clean-tree audits found no remaining reference, filename or retired asset path outside Git metadata and build dependencies. Validation passed 881 admin tests, 704 backend/shared tests, both Next typechecks and both production builds. The runner suite had already passed 410 tests before the final asset-only selection. PR #56 merged to `main` at `7382c91`; exact-main CI run `35460050483` passed.

Login lockup alignment, 2026-09-20: The runner login logo is left-aligned with the Mindanao eyebrow and headline. This follow-up uses the standard feature branch to staging to production promotion path. Staging validation and production promotion remain pending.

## Release-critical checklist

These are the minimum web and admin gates for the first real organizer. Close each against the same staging release revision, then verify its production configuration. A passing build or database-only test does not close an end-to-end gate.

| Priority | Gate | Evidence needed before launch | Current status |
|---|---|---|---|
| 1 | Payment and slot safety | Run the normal paid/refund journey and prove retries cannot create extra registrations, tickets or slot use. Keep payout fail-closed for any anomalous capture. | **MVP accepted:** production sandbox purchase/refund passed. Same-payment replays produced one ticket, one settled capture and one slot. A distinct second capture is durably quarantined before fulfillment, alerts platform staff and blocks payout. Automated operator refund/resolution is deferred to issue #51; the controlled pilot must escalate this rare case to the platform owner. |
| 2 | Financial accuracy | Reconcile the provider charge and fee with payment, organizer net, refund and settlement figures for the pilot payment path. | **MVP accepted for the tested GCash path:** production sandbox reconciled ₱100 gross, ₱2.50 PayMongo fee, ₱0 Race Pace commission, ₱97.50 organizer net, then a succeeded ₱97.50 refund and ₱0 payable. Admin Registrations, Payments and Settlement matched. Broader payment-method and export coverage remains post-MVP. |
| 3 | Identity, tenant isolation and privacy | Confirm runner and organizer access, account recovery and organization isolation. | **MVP accepted:** email/password, Google sign-in, organizer invitation and two-organization API isolation passed. Foreign events, registrations, payments, exports and Passport/contact data were hidden. Additional hosted role-change coverage remains post-MVP. |
| 4 | Organizer-to-ticket journey | Complete organizer creation, event/category/waiver publishing, runner Passport, payment, email and QR ticket. | **MVP accepted:** staging and protected production both completed the core flow. Production created one paid QR ticket, delivered its email, then invalidated the QR and released the slot after refund. Assisted and group checkout stay unavailable unless separately enabled and tested. |
| 5 | Race-day operation | Validate QR, kit release and optional check-in behavior needed by the pilot. | **MVP accepted for the tested workflow:** signed scanner input, duplicate/forged/wrong-event/refunded rejection, kit release, optional check-in and reversal passed. Physical-camera and marshal-device exercises move to organizer acceptance. |
| 6 | Essential delivery and support | Verify account access, ticket delivery and monitored organizer contact routes. | **MVP accepted:** branded authentication and ticket emails arrived, and inquiries/onboarding routes exist. A dedicated refund notice, failed-delivery recovery and remaining appearance checks are post-MVP work. |
| 7 | Production cutover and acceptance | Complete live configuration, scoped prelaunch QA-data cleanup, backup checks and the explicit decision to remove Coming Soon. | **MVP released:** PR #54 is deployed, the public runner and authenticated admin are open, production QA data is removed, and the backup plus Storage snapshot are recorded. PayMongo live and webhook secrets are installed, QR Ph is enabled, and unsigned webhooks fail closed. Pilot-organizer content sign-off and the owner-led first live checkout remain. |

### Conditional launch scope and later work

| Work | Launch rule |
|---|---|
| Assisted/non-member registration | Release-critical if the first organizer will accept assisted entrants. Otherwise keep it unavailable and state that limitation before onboarding the organizer. |
| One-payment group checkout with multiple QR tickets | Defer only while both group feature flags stay off and the pilot organizer accepts one runner per checkout. Test capacity, distinct QR tickets and group refunds before enabling it. |
| Google sign-in | Defer only if the Google entry point is hidden and email/password account recovery is proven. |
| Extra report formats, visual email polish, dark-mode appearance and mobile app | Follow after launch once essential financial exports, message delivery and web/admin usability pass. |

No sample or QA data may be added to production after launch. The pre-launch QA records were removed on 2026-09-19; preserve legitimate data and access.

Validation snapshot, 2026-09-18: exact `staging` revision `d7136b4` passed runner/admin typechecks, 408 runner tests, 877 admin tests and 692 backend tests against local Supabase with the running local function environment. Its exact-merge GitHub run `35334667051` passed. The current staging database has one organization, six events, six registrations and six payments. Read-only baseline checks found no paid ticket without a QR, category slot-count mismatch or unbound pending checkout. Six PayMongo test-mode checkout sessions independently matched the staging ledger's paid amounts, actual fees and provider nets; both recorded refund amounts were `succeeded` in PayMongo. Two paid-return replays produced the same ticket and one settled capture with delivery count two, without extra payment or slot use. Six admin payment rows matched the database. The downloaded six-row Payments CSV matched SQL on ₱1,902.57 original gross, ₱47.56 processor fees, ₱54.00 original platform fees and ₱169.00 refunded; its retained gross ₱1,728.07, current platform fees ₱51.00 and current organizer net ₱1,632.01 matched the admin page. The organizer's platform-only review route returned 404. A new inquiry test email was marked Delivered by Resend, but member inbox placement is not fully verified. Both hosted Supabase projects report healthy. Unauthenticated staging requests redirect to Vercel sign-in; production web/admin GET routes redirect to Coming Soon and an unknown POST returns 503. These observations do not close the distinct paid-capture, second-organization, delivery, hardware, payout or production-release gates.

Next action: create the first real organizer and event in production. The owner will then run the first QR Ph checkout and confirm the ticket, actual provider fee, admin ledger and settlement figures. Issue #51 and the remaining polish stay in the post-MVP backlog.

Staging waiver-guard rollout, 2026-09-18: PR #36 passed both exact-merge GitHub checks and was squash-merged as `663b93b`. A follow-up migration requires a published same-organization waiver before an organizer opens a new event, while the Edge checkout rejects draft and legacy waiverless events. The staging migration dry run listed only `20260918130000`, the push applied it, and a second dry run reported the remote database up to date. `registrations-checkout` is ACTIVE v9 with JWT verification on. The runner receives a specific server error. A separate source-level `fake-checkout` guard requires the local CLI runtime even if the PayMongo key is absent. After a fresh local database reset and serving the changed worktree's Edge code, the full backend suite passed 700/700 across 85 files; runner typecheck and 409/409 tests passed, and admin typecheck and 879/879 tests passed. Browser inspection after staging deployment found that a legacy open event still displayed generic waiver text. PR #37 passed exact-merge CI on its third attempt after two isolated Supabase startup failures, merged as `320bfc2`, and its staging site is Ready. The browser now shows “Registration is temporarily unavailable” before the wizard on that event, without waiver acceptance or checkout. The earlier `d7136b4` financial validation snapshot remains historical evidence; it does not cover a new paid purchase on `320bfc2`.

Capacity-grant audit, 2026-09-18: staging had granted `authenticated` table-wide `INSERT` and `UPDATE` on `categories`, including the payment-derived `slots_taken` column. The organization RLS policy restricted rows, not columns. PR #38 passed both exact-merge CI runs and merged as `661763c`. Its migration replaced those grants with the exact category-editor columns and confined both legacy slot helper functions to `service_role` with fixed search paths. After a local database reset, an organizer could edit a category label but could neither update `slots_taken` nor insert a nonzero count; a service-role update succeeded. Focused admin event and grant tests passed 12/12. Staging migration `20260918140000` applied and a second dry run reported up to date. Hosted SQL confirms `authenticated` cannot update or insert `slots_taken`, can update `label`, and cannot execute `increment_slot`; `service_role` still can. The mutable-search-path advisor warning disappeared. Supabase's [security advisor](https://supabase.com/docs/guides/database/database-linter) still reports other privileged functions and Auth settings; the helper functions used in RLS return data scoped to `auth.uid()`, but the remaining warnings need a separate security review.

Production test-endpoint removal, 2026-09-18: the hosted `fake-checkout` Edge Function had been ACTIVE with JWT verification off. The production-only deployment was deleted after confirming the live PayMongo flow uses `payment-session` rather than that local sandbox provider. A fresh function listing reports 12 remaining functions and no `fake-checkout`; an unauthenticated GET to its public URL returns 404. The local source stays for local integration tests. No production payments or data rows changed.

Two-organization staging API test, 2026-09-18: isolated synthetic organizations, admin/runner users, draft events/categories, Passports with synthetic contacts, pending registrations and zero-amount failed fake payments were created for both sides. Authenticated API reads returned each caller's own rows and hid foreign rows in `events`, `categories`, `registrations`, `payments`, `admin_registrations_v`, `admin_payments_v` and `runner_passports` in both directions. Forged foreign organization/event filters on the export backing views returned zero rows; foreign `admin_registration_emails` calls returned no foreign contact data. The two CSV route test files passed 39 tests. Cleanup restored the baseline of one organization, six events, six registrations, six payments, five Passports, three Auth users and one staff role. Hosted browser CSV downloads were not exercised. No production data or real payment was touched.

Production record inspection, 2026-09-18: read-only counts still show one organization, two open events, one paid registration/payment and five Auth users. `QA Readiness Road Run 2026` holds the paid PayMongo record of ₱1,000; `Pulangi Half Marathon` has no registration. These records need ownership and retention decisions before scoped pre-launch cleanup. Neither event nor payment was changed.

Production configuration inventory, 2026-09-18: the staging function list includes `expire-paymongo-checkouts` and its project has `PAYMENT_EXPIRY_WORKER_SECRET`; both are absent in production. PayMongo key, webhook secret and Resend key names exist in both projects, but listing names cannot prove credential validity or live/test mode. Production checkout must remain closed until the worker, scheduler, webhook and secret modes are verified together.

## Original 20-item launch checklist

| Order | Work | Status |
|---|---|---|
| 1 | Domain and hosting connections | Done; staging domains serve HTTPS. |
| 2 | Separate staging infrastructure | Done; isolated Supabase and Vercel environments. A fresh generic runner preview displayed staging-only QA events, and the fresh admin preview's Google OAuth target was staging Supabase. Older immutable previews need avoidance. |
| 3 | Staging database migrations | Current through `20260918140000`; the waiver publication and category grant guards are installed. A fresh staging CLI dry run reported no pending migrations. |
| 4 | Database permissions and organization isolation | Partial; rollback-only two-organization write isolation and hosted two-organization authenticated API reads passed. Foreign event, registration, payment, admin-view, export-view and Passport contact rows were hidden in both directions. Hosted browser export downloads remain. Staging also denies organizer insert/update of `slots_taken` while preserving normal category edits. |
| 5 | Google authentication | Staging provider and its separate OAuth client are enabled, but Google audience is Testing with zero test users. Publishing is disabled until consent branding is complete; live login remains unverified. |
| 6 | Resend domain verification | Done; notify.racepace.com.ph verified. |
| 7 | Authentication and ticket emails | Partial; Auth delivery, automatic single-ticket delivery and desktop branding passed. Explicit ticket plain text needs a fresh inbox test. No refund-notice sender exists in the current refund path. Mobile appearance, remaining notice types and resend behavior after a partial refund remain. Google Workspace Gmail is activated for racepace.com.ph. Inquiries and onboarding addresses route to separate Google Groups with team@, jayson@ and mondel@ as members, each subscribed to every email. Both external test messages reached the group archives, but their first forwarded copies were marked spam. After correcting a false spam flag for Jayson, a natural onboarding follow-up reached all three mailboxes. A natural inquiry follow-up reached team@ and jayson@, but Mondel's copy was still marked spam. Mondel must review legitimate inquiries in Spam or the group archive until inbox classification is reliable. Outbound alias sending and Workspace DKIM remain. |
| 8 | Backend secrets and functions | Production payment and group-payment functions are deployed with the live PayMongo and webhook secrets. The webhook rejects unsigned requests. Capture review, payout hold and provider-confirmed expiry remain in place. |
| 9 | PayMongo test payments and webhooks | The sandbox purchase/refund path passed and reconciled with the ledger. Production Hosted Checkout now allows GCash, card, Maya, GrabPay, BillEase and QR Ph through server allowlists. The owner-led live QR Ph checkout remains intentionally untested. |
| 10 | Worker schedules and delivery monitoring | The five-minute staging worker processed a known expired PayMongo session and, separately, an unbound synthetic hold. For the unbound case it recorded `missing_session_ref`, inserted platform notification rows and retained the pending hold with zero captures. Both fixtures were removed. Alert display in the admin browser remains. |
| 11 | Deploy staging web and admin | PRs #36–38 merged into `staging`; site and admin Vercel deployments are Ready and aliased to custom staging domains. The legacy waiverless runner page now blocks registration. The organizer account receives 404 on the platform review route; platform browser display remains. |
| 12 | Staging and production CI/CD | PR #54 merged to `main` at `400da69`; both GitHub validation jobs and both Vercel previews passed. The production runner and admin custom domains serve the released applications. |
| 13 | Test accounts, organizations, events, and registration | Partial; one staging organizer, six synthetic events and six paid self-registrations. Google, browser second-org and non-member cases remain. |
| 14 | Complete and test non-member/group checkout | Runner-side same-category group flow is deployed to staging but remains disabled by flags. Signed-in runner entry showed no group link and its direct group route returned 404. Staging has zero group orders, lines, attempts and captures. Sandbox one-payment/multiple-QR flow before enabling flags. |
| 15 | Test fees, zero commissions, refunds, and payouts | Partial; six PayMongo ledger rows reconcile by status against independent provider GETs, including two succeeded provider refunds and the ₱100 pilot GCash capture. The downloaded Payments CSV matches database totals and the live admin summary. Rollback tests covered extra-capture payout hold; a paid-return replay safely incremented capture delivery count without adding money or slots. A real expired-session worker path passed; sandbox create retries proved assumed idempotency unsafe. Distinct late/extra paid captures, unbound-session resolution, other methods, group refunds, payout console and bank transfer remain. |
| 16 | Test QR tickets, kit releases, and optional check-ins | Partial; signed ticket scanner input, duplicate rejection, forged-token rejection, wrong-event rejection, kit release and check-in/reversal passed. Fresh browser read-back showed the collected-kit ticket, one current check-in, and retained reversal history. A prior staging test showed the disabled station and ticket while kit collection stayed visible; its database setting was restored and audit recorded. The admin form's native confirmation prevented automated save verification. Refunded ticket is invalid. Physical camera scan was skipped; marshal scope remains. |
| 17 | Verify reports, exports, privacy, and usability | Partial; the live six-row Payments CSV now downloaded and its original gross, processor fee, original platform fee and refund totals match SQL. Retained gross, current platform fee and current organizer net match the admin page. The refunded future race is no longer in Upcoming after PR #27 deployed. The ticket shows Team Name instead of Bib; existing QA entries have no saved team, so a populated live value remains unverified. Other exports, two-org privacy boundaries and mobile layout remain. |
| 18 | Finalize production configuration and deployment | Done for the MVP. PR #54 is deployed, Coming Soon is disabled, QR Ph is available through PayMongo Hosted Checkout, and the production webhook fails closed without a valid signature. |
| 19 | Remove confirmed production test data | Done. Production has zero organizations, events, registrations, payments and QA notifications. Named accounts and the platform super-admin were preserved. |
| 20 | Final launch checks and organizer acceptance | In progress. Create the first real organizer and event, then complete the owner-led QR Ph purchase, ticket and ledger review. |

## Eight launch gates (web and admin)

Each gate requires staging evidence on the exact release revision. A green build, a database-only test, or a provider sandbox success alone does not close a gate.

| Gate | Must prove | Current result |
|---|---|---|
| 1. Payment and slot safety | Success, decline, retry, abandoned/expired checkout, late and duplicate capture; correct slot/payment/ticket counts | Partial. GCash success/decline, rollback-only capture replay and two nonempty staging expiry-worker cases passed. Two fresh paid-return replays preserved one ticket, payment, capture and slot while capture delivery count reached two. Identical sandbox checkout-create retries minted distinct sessions on both v1 and v2. The fail-closed fix, read-only queue and category slot-write grant fix are deployed. Distinct late/extra paid captures, platform browser display and guarded resolution remain. |
| 2. Financial reconciliation | Provider charge and actual fee equal ledger, settlement, exports and zero-commission pilot terms for each enabled method | Partial. Six independent PayMongo sandbox GETs matched ledger charge and actual fee, including zero-commission and fixed-price GCash; both refunds show succeeded provider amounts. Six-row Payments CSV totals match SQL and admin. Other methods, payout accounting, tax treatment and broader exports remain. |
| 3. Refund and payout protection | Full/partial refund, ticket/slot state, unresolved-capture payout hold, statement and bank-transfer accounting | Partial. Two refunds and local/staging payout holds passed; payout console, bank transfer and post-statement refund remain. |
| 4. Organization isolation | Two organizers cannot read or change each other's private records through browser, API or database | Partial. Rollback-only database isolation passed. Fresh anonymous access to registrations/payments was denied, a signed-in role without a user saw zero private rows, and the organizer's platform review URL returned 404. Persistent staging has only one organizer; browser two-org matrix remains. |
| 5. Complete registration | Account, Google/email authentication, Passport, organizer waiver, payment, confirmation email and ticket | Partial. Single-runner email/password path passed. New staging events require a published organizer waiver; legacy waiverless checkout is rejected and its runner page now explains why. Google audience is Testing with zero test users; field/error matrix and mobile inbox remain. |
| 6. Non-member and group checkout | One payer, same category, multiple distinct participants and QR tickets, capacity and refund behavior | Incomplete; feature flags remain off. |
| 7. Race-day operations | Scoped staff, kit release, optional check-in, real camera QR, duplicate/wrong-event/refunded rejection and reversal | Partial. Scanner-input protections and kit/check-in flow passed. Fresh browser read-back shows the collected-kit ticket, one current check-in and history. Prior staging browser showed the disabled station and preserved history; database audit recorded disable and restore. Admin-form save, marshal scope and optical scan remain. |
| 8. Production cutover | Exact configuration, data cleanup, backup/restore, monitoring, smoke checks and organizer acceptance | Partial. Coming Soon gate and database backup inventory passed. The hosted migration inventory shows 38 versions in staging that production lacks. Storage recovery, restore drill, live configuration and acceptance remain. |

## Detailed evidence table

| # | Work | Status | Next action / completion evidence |
|---|---|---|---|
| 1 | Domain and Vercel routing | Done | Production web/admin and both staging domains accepted by Vercel. Verify HTTPS after deployment. |
| 2 | Isolated staging infrastructure | Done; fresh site/admin generic previews verified | Singapore Supabase project and two Vercel staging environments created. Production and preview variable scopes are separate. A fresh generic runner preview loaded staging-only QA events. The fresh generic admin preview's Google OAuth redirect targeted staging Supabase `pepbmqomiailnnvvwupz`, proving its client configuration. Existing older preview builds retain their original build-time variables. |
| 3 | Staging database migrations | Done for this revision | Unbound alert `20260918115000` and platform review query `20260918120000` applied after capture inbox and provider-confirmed expiry. A subsequent staging-only dry run reports current. Production schema was not changed. |
| 4 | Database permissions and tenant isolation | Partial | Public tables have RLS. Staging organizer RPC call to open a payout returned 42501; `anon` cannot execute mark-paid and the private before-groups helper remains inaccessible to authenticated users. A rollback-only staging transaction proved each of two organizer admins could read its own private draft but not the other's draft, registrations or payments, and could not update the other's event. Browser-role tests and broader grants remain. |
| 5 | Authentication URLs and Google clients | Partial | Exact staging/production URLs saved; separate staging Google client enabled. Verify production provider and login behavior. |
| 6 | Google consent branding and audience | Blocked | Test-user save did not persist; branding incomplete prevents public publishing. Resolve and verify audience/scopes before launch. |
| 7 | Resend domain verification | Done | Resend dashboard verified notify.racepace.com.ph on 2026-09-17. |
| 8 | Email credentials and SMTP | Staging Auth and automatic ticket delivery passed; shared organizer routing partially verified | Separate sending keys and Auth SMTP configured. App-generated organizer invitation, password-change notice and paid-runner ticket were Delivered in Resend with branded HTML. The ticket email's logo and QR loaded in desktop preview. Explicit single/group text versions are deployed, but a fresh inbox check and real mobile-client comparison remain. Google Workspace activated Gmail for racepace.com.ph. Inquiries and onboarding are Google Group aliases with three members, each set to receive every email. External messages reached both group archives. After marking one Jayson onboarding message as legitimate, a follow-up onboarding message reached all three mailboxes. A follow-up inquiry reached team@ and jayson@ but was marked spam for mondel@. The public links are live behind Coming Soon. Mondel's inquiry inbox reliability, outbound alias sending, DKIM and branded replies remain. Resend's notify.racepace.com.ph sending setup was not changed. |
| 9 | Backend secrets and Edge Functions | Partial; fail-closed fix deployed to staging | Staging ticket signing, internal email authorization and public ticket URLs are configured. Payment-session v10 refuses to recreate an unbound PayMongo session and reuses a saved link. Payment-verify v10 rechecks available paid-session captures without downgrading an already-paid ticket. Both are Active with JWT verification; unauthenticated POSTs returned 401. A service-only capture inbox, payout hold and provider-confirmed expiry remain active. The unbound alert and platform review RPC are installed. A rollback-only query verified platform visibility and organizer denial. A scheduled nonempty worker run inserted two platform notifications while preserving the pending hold. Authenticated function paths, lost-response resolution, late/duplicate paid captures and browser alert display need controlled live tests. Group delivery remains gated. |
| 10 | PayMongo test payments and webhooks | Six paid paths, callback replay and failed-to-paid retry passed; GCash failure exit needs review | The sixth checkout validated the pilot's fixed price: PayMongo test GCash captured ₱100 and returned to a signed ticket. Provider webhook supplied a ₱2.50 actual fee; ledger and settlement both calculate ₱94.50 organizer net after ₱3 Race Pace fee. Local callback parsing now enumerates every paid ID in one session, and rollback-only database tests hold payout for an extra ID. The updated staging webhook is Active; controlled duplicate/late-capture tests, GCash failure exit and Maya/card remain. |
| 11 | Worker schedules and email events | Partial | Three DB jobs now exist, including `paymongo-checkout-expiry` every five minutes with a Vault bearer token. Authenticated POST and two scheduled HTTP responses returned 200 with no candidates. A nonempty expired-session run passed on 2026-09-18; uncertain provider responses and alerting remain. |
| 12 | Staging web/admin deployment | Git-backed Ready; broader tests ongoing | PR #32 merged as `ad4dd08`; site `dpl_3Jj5r1E6PvDXipskVJsQBqh1dNsr` and admin `dpl_5HBea4tsayQan27o5thnkSAJAJ8z` are Ready and aliased to staging domains. The organizer browser receives 404 on the platform review route. Authenticated payment-path and platform-browser checks remain. Anonymous access remains protected by Vercel sign-in. |
| 13 | CI/CD for staging and production | Exact-merge validation green; protected-branch gate active | PR #33's exact-merge CI run `35322672552` passed on `1247384`. `main` and `staging` require pull requests and a strict passing status, enforced for admins. Force pushes and deletions are disabled. Production approval remains. |
| 14 | Accounts and access tests | Partial staging pass | Runner/admin recovery and sign-in work. App-generated organizer invitation was delivered and redeemed; invited admin reached its own staging Events page without platform navigation. Google login, expiry/reuse and two-organization isolation remain. |
| 15 | Organizations and event tests | Six synthetic staging events created | The sixth, `[TEST] Fixed-Price Fee QA Race`, is open at ₱100. Its name, category and description match the pilot pricing rule. One synthetic registration is paid and one of four slots is taken. The prior zero-commission/pass-on event is closed. Staff/marshals and browser second-organization isolation remain. |
| 16 | Race Passport and registration tests | Six paid staging self-registrations | With specific approval, Staging Runner accepted the test-only waiver for the fixed-price event and paid ₱100 in PayMongo test mode. The runner reached a signed QR ticket. Database confirms one ticket, one payment and one slot, with 10,000-centavo gross, 300-centavo frozen Race Pace fee and absorb mode. The revised admin table shows Team Name; live CSV bytes remain unverified. PSGC/shipping, duplicate prevention and non-member identity remain. |
| 17 | Non-member and group checkout | Deployed to staging, gated | Same-category runner UI and group backend are deployed, but site and Edge Function flags remain off. A signed-in runner saw no group link, a direct route returned 404, and staging has zero group orders, registrations, payment attempts and captures. Exercise one payer, distinct self/managed Passports, individual waivers and tickets, expiry and refund in the sandbox before enabling. |
| 18 | Payments, fees and pilot commission | Fixed-price GCash capture reconciled; other methods and lifecycle controls pending | The new ₱100 absorb checkout captured exactly ₱100 through PayMongo test GCash. Its paid webhook reported ₱2.50 actual processor fee and ₱97.50 processor net. The ledger deducts ₱3 Race Pace fee and gives ₱94.50 to the organizer. Runner ticket, organizer Payments row and settlement waterfall agree. One payment row, ticket and slot exist. A frozen provider request is deployed. The sandbox showed that an identical retry with the same idempotency key minted a second session; staging now fails closed for an unbound session. Authenticated refusal still needs a live test. A capture inbox, payout hold and explicit provider-confirmed expiry are active in staging. A rollback-only extra-capture test returned reconciliation-required and raised the payout blocker; afterward the QA capture count and blocker count were zero. A live nonempty expiry-worker case passed. Late/duplicate paid capture and unbound-session resolution remain. Maya/card remain. The historical pass-on QA capture is not pilot policy. The “Taxes and fees” label does not calculate or establish tax; define tax treatment before production. |
| 19 | Refunds, payouts and financial reports | Full and partial PayMongo test-mode GCash refunds reconciled; payout console still pending | The ₱94.50 full refund released its slot and invalidated its ticket. The ₱74.50 flat-fee partial refund succeeded under provider refund `ref_eYERUAqCpsVq2Ug8TxUGw7kT`, retained ₱20 organizer net, kept its paid registration and one slot, and left the QR ticket active. Admin Payments shows retained gross ₱25.50, platform fee ₱3, organizer net ₱20 and refunded ₱74.50 for that event. Its settlement waterfall is ₱100 − ₱3 − ₱2.50 − ₱74.50 = ₱20. Read-only SQL confirmed one succeeded refund request and audit record. Earlier rollback-only payout tests verified statement arithmetic and the future-event hold without leaving statements or stamps. Live bank transfer and console payout recording, group refunds, pass-on and zero-commission pilot remain. |
| 20 | QR tickets and race-day operations | Scanner-input and optional-mode browser paths passed on staging | Paid runner ticket and delivered Resend email show QR, runner/category and ₱1,400 total. Email logo and staging banner render. QR endpoint returns PNG and points to staging. Organizer released the synthetic kit; runner ticket says Collected. Manual check-in and reversal passed. Signed scanner input, duplicate, forged and wrong-event checks passed. With the test event disabled, the station hid scanning and retained seven history actions; the runner ticket stated check-in was not required and still showed the collected kit. The database restored check-in afterward and recorded two setting audit entries. The admin form's native confirmation interrupted automated save, so its save path remains unverified. The user skipped physical camera scanning. Group tickets and marshal permissions remain. The earlier delivered email had collapsed plain text; the deployed fix needs a new delivered-message check. |
| 21 | Remaining reports, exports and usability | Full-refund CSVs verified; partial-refund pages reconciled | Browser downloads previously verified Payments, Registrations and Settlement CSV after the full refund. The partial-refund Payments and Registrations pages and Settlement waterfall now reconcile to the provider ledger. Settlement CSV unit coverage asserts that partial refunds retain ₱20 payable and report the refund separately; the in-app browser blocked a fresh live CSV navigation, so its downloaded bytes remain unverified. “Gross” aggregate cards were relabeled “Retained gross” because their SQL figures subtract refunds, while transaction rows keep the original ₱100 charge. PR #27 removed the fully refunded entry from Upcoming; live counts are Upcoming · 5 and Inactive · 1. Other exports, privacy boundaries, mobile layout and error states remain. |
| 22 | Production configuration and release | Full code deployed; public launch held | Production site `dpl_7uRvjyQUqN7DbHDP6txbzagoVegM` and admin `dpl_4cBsPg1765h6rgxeWVfULMBM9uDY` are Ready from `c1735cb` on their custom domains. Public app GETs redirect to Coming Soon. The protected production refund row still needs a direct visual check. Live PayMongo mode, backups/Storage, QA-data cleanup and organizer acceptance remain. |
| 23 | Production test-data cleanup | Required before launch | Identify/remove confirmed sample records, accounts and uploads with scoped review. Preserve reference data and legitimate admin access. |
| 24 | Final launch verification and operations | Partial; public launch blocked | Both Supabase projects previously reported ACTIVE_HEALTHY, and the production database backup inventory showed seven completed backups. Database backups exclude Storage objects; refresh the inventory and complete a restore drill. Check alerts and live-mode secrets, inspect essential CSV bytes, review QA data and obtain organizer acceptance before lifting Coming Soon. |
| 25 | Resend MCP connector | Optional / blocked | OAuth issuer validation failed. Resolve without weakening checks; dashboard/API setup can continue independently. |

## Production data rule
After cutover, production is real-data-only; never insert sample/demo/QA data. Synthetic tests belong in staging/local. The prelaunch organization and event cleanup is complete. The latest readback found four named accounts, zero organizers, zero events, zero registrations, zero payments, zero event/organization images and 12 preserved profile images. Do not classify or delete the remaining identity records from counts alone.

## Evidence
- [Hosted setup history](2026-09-17-hosted-setup.md)
- [Staging bootstrap plan](../plans/2026-09-17-staging-bootstrap.md)
- [Detailed end-to-end checklist](../plans/2026-09-15-web-admin-e2e-checklist.md)
- [Group checkout architecture](../specs/group-checkout-architecture.md)

## Update log
- 2026-09-18: Replaced the team-only aliases with separate Google Groups for inquiries@ and onboarding@. Each group has team@racepace.com.ph, jayson@racepace.com.ph and mondel@racepace.com.ph as members with Each email selected; external posting is allowed while conversation access is members-only. Two external synthetic messages reached the group archives but Google marked all six forwarded recipient copies spam. A natural inquiry reached team@ and jayson@ inboxes while Mondel's copy was marked spam. A natural onboarding request reached team@ while Jayson and Mondel's copies were marked spam. Marked the legitimate onboarding message as Not spam in Jayson's mailbox. A subsequent natural onboarding message was delivered to all three mailboxes; a subsequent natural inquiry was delivered to team@ and jayson@, but Mondel's copy was still marked spam. The group archives preserve all messages, while Mondel's inquiry inbox remains unreliable. Merged the two-link Coming Soon change in PR #34 at `ce439db` with exact-merge CI `35326775600` passing. Built a narrow production snapshot from holding-page commit `5511859` plus only those links, pushed it as `6bdd35b`, promoted Ready site deployment `dpl_C7uNGW2PVW4vtMc1LVMv2XBAf953`, and verified the live mailto targets. Production root and race GETs still redirect to Coming Soon; a registration POST returns 503. No database or payment settings changed.
- 2026-09-18: Activated Google Workspace Gmail for racepace.com.ph after adding the root-domain mail exchanger in GoDaddy. Kept the existing root SPF record, which delegates to a Google SPF include, and left Resend's notify subdomain unchanged. Created inquiries@ and onboarding@ as aliases on team@racepace.com.ph. Sent separate plain delivery checks from support.racepace@gmail.com to each alias; both appeared in the team inbox. Outbound alias sending and DKIM remain. Prepared separate organizer onboarding and general inquiry links on the Coming Soon page; production deployment and live link verification remain.
- 2026-09-18: A signed-in staging runner still saw a fully refunded future entry under My Races → Upcoming · 6. The partition used only the event date. Added a distinct Inactive tab for refunded, cancelled, expired and lapsed-pending entries, preserving their history and preventing the invalid ticket from appearing as an active booking. Changed the footer's obsolete “bib on your phone” copy to “race pass.” Focused My Races tests passed 15/15 and site typecheck passed. The fix is in PR #27 pending staging deployment and responsive browser verification. The deployed PR #26 ticket showed Team Name instead of Bib; existing QA entries have an empty frozen team name, so the live populated case remains unverified. The signed-in group entry offered no group link, its direct route returned 404, and a database read-back found no group orders, registrations, attempts or captures.
- 2026-09-18: Rechecked six staging PayMongo payments using the current refund invariant. Four paid rows, one fully refunded row and one partially refunded row all have `processor_fee_source=actual` and zero status-aware ledger inconsistencies. Fully refunded payments deliberately retain their original `net_to_org` as a historical clawback basis; partial refunds keep the original gross amount and store the returned amount separately. A single arithmetic formula across all statuses would falsely flag these designed states. Production Coming Soon and staging Vercel sign-in still returned their expected public GET and production POST responses after the staging merge. A follow-up ticket fix is in PR #26: show the registration's saved Team Name instead of the obsolete Bib field. Focused ticket and admin confirmation tests and both typechecks passed; live ticket verification awaits that PR's deployment.
- 2026-09-18: Merged PR #25 into the protected `staging` branch as `2a50561` after both PR-head GitHub validation jobs and both Vercel previews passed. New site `dpl_AV7yNrHBEZn13vHUH113mAWeKyc2` and admin `dpl_7Ej36cKVjdLSdNGnnfhdvtuLAVvV` deployments are Ready and aliased to their staging custom domains; exact-merge run `35304892262` passed. Signed-in admin reload showed Team Name in Registrations. The browser's native confirmation interrupted a form-save attempt, so a narrowly scoped staging database update disabled check-in on the existing `[TEST] Race Pace Staging Trail Run`. The admin station showed “Check-in not required” and preserved seven history actions. The runner ticket kept its QR and collected-kit status while stating check-in was not required. The database restored check-in afterward; read-back showed one current check-in and two setting audit entries. A fresh station reload showed its scanner again. The browser did not yield downloaded CSV bytes. A production-only dry run listed 34 pending migrations; none were applied and production data was untouched.
- 2026-09-18: Combined local validation of the new runner group interface, optional check-in, Team Name report labels, refund capture lookup, capture-review notifications and registration deletion guard passed 404 site tests, 870 admin tests, 680 backend tests and both TypeScript checks. The backend suite required the same local Edge secret file as the running function server; its first run against a different file failed, then the aligned run passed. Applied only `20260918100000`, `20260918113000` and `20260918114000` to staging Supabase. Read-only staging SQL confirmed both check-in columns, the mode and delete helper functions, and the capture-review trigger. Migration-history read-back shows all three versions and a fresh staging dry run reports current; the CLI's initial pg-delta cache warning did not prevent application. Eight affected Edge Functions were redeployed to staging and list as Active. Commit `b9d1307` is pushed with draft PR #25 into `staging`; both Vercel preview builds and both GitHub `web-admin-validate` jobs passed for that exact head. A local PR review found no confirmed high or critical code defect and identified live group/provider acceptance as remaining release gates. The new group interface remains disabled. Staging Google OAuth is Enabled and its separate client has the correct Supabase callback, but the Google audience is still Testing with zero test users and publishing disabled until branding is complete. The staging Payments page reconciled six provider rows to ₱1,728.07 retained gross, ₱51 platform fees, ₱1,632.01 organizer net and ₱169 refunded; the Codex browser blocked the CSV route, so exported bytes remain unverified. Production database and app were untouched.
- 2026-09-18: Started the eight-gate web/admin launch matrix with parallel payment, group-registration and admin/race-day workstreams. Read-only production cutover audit found production and staging Supabase ACTIVE_HEALTHY. The production project has seven completed physical backups (latest 2026-09-17 19:26 UTC); staging has two (latest 2026-09-17 17:53 UTC). Neither has point-in-time recovery. Supabase documents that database backups do not restore Storage object bytes, so a separate file recovery plan and restore drill remain. Production still has five auth accounts, one organization, two events, one registration, one payment and 48 Storage objects; none were changed or classified as disposable. The Coming Soon gate returned 307 for both production app GETs and 503 for both write probes; anonymous staging app GETs returned 302 to Vercel sign-in. Supabase security advisors at error level reported no issues for either hosted project. Production data and deployment were untouched.
- 2026-09-18: Verified the five-minute staging PayMongo expiry job's latest run succeeded. The staging database had zero pending registrations, capture reviews and unresolved expiry attempts, so a nonempty provider test was not possible from existing records. A direct synthetic pending fixture without waiver acceptance was rejected by the database's `waiver_version_changed` trigger; read-back found zero fixture registrations and payments. The nonempty worker test requires a legitimate test-only registration and waiver acceptance. A rollback-only staging call observed the existing paid GCash ID twice: `state=settled`, `delivery_count=2`, and zero payout blockers. After rollback, the new capture row count was zero. This verifies the database replay path, not the live callback. Vercel confirmed the exact `ef95188` staging site/admin deployments are Ready and aliased to both staging domains. GitHub run `35296413450` passed its `web-admin-validate` job on that exact commit. Enabled protection on `main` and `staging`: pull requests, strict passing `web-admin-validate`, admin enforcement, conversation resolution, no force push and no deletion. API read-back confirmed both protections. Vercel lists separate preview and production variables for each app. A fresh generic runner preview loaded the staging-only QA race and other synthetic staging events. The fresh generic admin preview's Google OAuth redirect targeted staging Supabase `pepbmqomiailnnvvwupz`. No Google account was selected or signed in during that read-back. Production database and application were untouched.
- 2026-09-18: Corrected a Vercel preview isolation gap in both site and admin projects. Their production variables now target production only. Generic preview variables were recreated with the decrypted custom-staging values, while custom staging remains separate. Future preview deployments use staging Supabase; older immutable preview builds can still retain the former build-time configuration and should not be used for QA. Fresh deployments and read-back are pending. No production deployment or database write occurred.
- 2026-09-18: The dedicated `staging` branch produced Ready site/admin deployments at `dfbc870`, each aliased to its staging custom domain. Signed-in runner ticket and admin check-in loaded after reload; unauthenticated routes still redirect to Vercel sign-in. The staging expiry cron completed twice and pg_net recorded HTTP 200 for both scheduled requests. Grants expose capture/expiry RPCs to service_role and deny authenticated runners. A rollback-only staging test inserted a synthetic extra paid ID, saw `reconciliation_required` and a payout blocker, then rolled back; read-back found zero QA captures and blockers. Real duplicate/late PayMongo callbacks and nonempty expiry remain unverified.
- 2026-09-18: Published the reviewed source as `1291285` and fixed a clean-install CI dependency in `5113a75`. GitHub run `35294264786` passed the isolated database, all tests, typechecks and both builds. On staging Supabase `pepbmqomiailnnvvwupz` only, applied capture inbox and provider-confirmed expiry migrations; a follow-up dry run reports current. Payment-verify v7, payments-webhook v7 and expiry worker v1 are Active. The staging-only bearer secret is in Edge secrets and Vault; the five-minute cron job is active, and an authenticated manual invocation returned 200 with no candidates. The two Vercel staging custom environments now match the dedicated `staging` branch. A Git-backed deployment and controlled money-path tests remain; production schema and app were not changed.
- 2026-09-17: Consolidated verified setup, remaining infrastructure, staging test coverage, CI/CD and production cutover into this ledger. No new deployment or provider changes in this update.

- 2026-09-17: Resend domain verified. Prepared sending-only staging key scoped to notify.racepace.com.ph; creation not submitted. Requested approval for separate environment keys and their Supabase storage. User preference saved: show updated progress table after each completed step.

- 2026-09-17: Approved staging/production Resend keys created with domain-scoped sending access. Custom SMTP enabled in both projects on smtp.resend.com:465. Backend email secrets saved and listed in both projects. Sender names distinguish staging. Actual email delivery and link behavior remain untested.

## Branded email design

| Work | Status | Next action |
|---|---|---|
| Existing email and logo inventory | Done | Auth/invitations use Supabase; ticket and group renderers exist. Separate receipt/refund/event-update notification delivery remains proposed. |
| Visual email proposal | Approved | Open ../previews/email-branding.html. Twelve variants, existing logo, desktop/mobile controls, staging banner. All sample data stays in local artifact. |
| Shared branded templates and Auth page audit | Six staging Auth types and two security alerts applied | Password/email-change alerts are branded and enabled on staging. Five other optional security types remain off. Production templates remain drafts. |
| Proposal versus implementation comparison | Auth and ticket source comparison recorded | See email-design-validation.md. The delivered single ticket matches the proposal shell in Resend's desktop preview. A fresh email must verify the explicit plain text. Group delivery, real mobile rendering and production comparison remain. Five proposed notices have no live triggers. |
| Logo loading | Staging Auth preview and Gmail desktop passed | Public staging Storage logo loads in hosted previews and delivered invitation/recovery. Real mobile Gmail remains to verify. |
| Delivery and link testing | Partial staging pass | Runner/admin resets completed. Organizer invitation and password-change notice delivered. Manually triggered and automatically triggered paid-ticket emails were Delivered in Resend with visible logo and QR; links target staging. New plain-text delivery, other notice types, expiry/reuse and mobile inbox remain. |

- 2026-09-17: Created local visual email proposal using the existing Race Pace logo. No live templates changed and no email sent. Preview does not prove inbox compatibility.

- 2026-09-17: Design approved. Added shared logo/header/footer and staging-banner support to existing single/group ticket renderers. Prepared 10 Auth HTML drafts (five types per environment). Existing three focused test files passed 11 tests before additional branding assertion; final focused ticket checks passed after it. Hosted templates not changed; EMAIL_ENVIRONMENT=staging still requires configuration. Auth invitation-link compatibility, plain-text parts, security notification inventory, remaining proposed notifications and delivery tests remain pending.

- 2026-09-17: Inspected staging hosted invitation, signup and recovery templates; all use ConfirmationURL, matching code handling. Applied approved branded HTML and TEST subjects to these three staging templates, preserving token placeholder. Hosted preview verified invitation and signup; real inbox delivery remains untested. Other Auth/security types and production templates remain pending. All seven optional hosted security notifications were disabled at inspection; reauthentication is an additional template to implement.

- 2026-09-17: Added the requested proposal-versus-implementation validation gate. It covers each variant, visual evidence, logo loading, desktop/mobile rendering, actual inbox delivery and functional links. Logo failure remains open; no design-fidelity or delivery pass claimed.

- 2026-09-17: Diagnosed dashboard img-src restrictions; original HTTPS PNG returned 200 and opened independently. Uploaded identical logo to staging email-branding Storage; applied URL to recovery, confirmation and invitation templates. Recovery screenshot visibly shows logo. Updated all five staging Auth source drafts. Production asset setup and delivered-inbox validation remain pending.

- 2026-09-17: Verified saved confirmation and invitation previews decode the logo at 700×372; confirmation screenshot visibly shows the logo. All three applied staging Auth templates now pass preview logo loading.

- 2026-09-17: Compared Auth source with approved proposal. Corrected taglines, heading margins, card border, banner and logo spacing in ten local drafts; applied three staging templates. Added email-design-validation.md covering all twelve variants and explicit remaining differences. Real staging invitation test started; delivery result not yet confirmed.

- 2026-09-17: First staging Auth invitation marked Delivered by Resend to support.racepace@gmail.com. Logo and corrected layout visible in delivered HTML preview. Plain-text part present with header spacing/repetition gap. Gmail rendering and auth-link completion pending; pending-verification staging test user created, no organizer membership granted.

- 2026-09-17: Actual invitation verified in Gmail Primary inbox; desktop logo and layout render correctly. Staging root returns Vercel DEPLOYMENT_NOT_FOUND, blocking authentication completion. Next: staging app deployment, then fresh application-generated Auth links. Mobile/dark-mode and plain-text formatting remain open.

- 2026-09-17: Both Next apps passed typecheck. Deployed an isolated source snapshot (excluding secrets/local env/mobile) to Vercel custom staging only. Runner dpl_EFHdjRFrUCkv29he91GCQWeGpb6K and admin dpl_3x3MgASykGRdRPv9AcEsNVMtWSgF are Ready with staging/staging-admin custom aliases. Production untouched. Runner home and password recovery verified in browser.
- 2026-09-17: Gmail desktop Dark theme preserves readable invitation content and logo; restored Default theme afterward. Mobile viewport request had no effect (DOM remained 1280px), so mobile is not passed. Browser policy blocked local file preview; no bypass attempted.
- 2026-09-17: Plain-text banner now separates from branding; decorative image alt removes repeated label. Applied invitation/recovery to staging; ten source drafts updated. Real recovery email 01a0afae-cde2-7108-8472-456e385e4868 arrived in Gmail; generated text retains the public image URL. Link successfully reached /auth/recovery and Choose a new password. User password-entry handoff pending; final session, reuse/expiry and admin invitation tests remain open.

- 2026-09-17: Admin staging login visibly loads with Race Pace branding. Applied plain-text-friendly markup to confirmation too; all three active staging Auth templates updated. Password update remains awaiting user handoff.

- 2026-09-17: Added the existing logo above the heading on both runner/admin recovery cards, across all states. 18 recovery tests passed; both typechecks passed. Staging redeploys dpl_2BaDqiU1kaNCcyoMxquUELRkFayo (site) and dpl_BL5Z5kieKjQwNQZD2hyUYm2kJXqC (admin) Ready; screenshots verify both logos. Production unchanged.
- 2026-09-17: User completed password update, UI confirmed success. User then signed in with the new password; protected /profile loaded with Race Passport and logout controls. Runner recovery happy path is passed through fresh authenticated access. Expiry/reuse, admin recovery and mobile email-client coverage remain pending.

- 2026-09-17: Granted the confirmed support test account staging-only super-admin role. Saved ADMIN_APP_URL for staging and deployed org-provision and org-members there. No production data changed. All six staging Auth types now use the branded template, including email change, magic link and reauthentication. Hosted previews show the logo after loading. A fresh admin recovery email arrived in Gmail and redirected to the branded staging-admin password form; user password entry is pending. Corrected shared recovery copy for admin recipients in staging and both local drafts. iPhone Gmail is available, but the support test inbox is not signed in there; real mobile-client rendering remains open.
- 2026-09-17: Set staging EMAIL_ENVIRONMENT and aligned single/group ticket renderer source to the approved email shell. Staging ticket branding selects the public staging Storage logo. Per-participant QR and links remain distinct; focused email tests passed. The ticket functions have not been deployed or delivered, so inbox visual parity is still open.
- 2026-09-17: Branded and enabled password-changed and email-address-changed security notices on staging. Hosted previews show the logo, and the email-address notice retains both address placeholders. Production was not changed; triggered inbox delivery remains unverified.
- 2026-09-17: User confirmed staging admin password update and sign-in work. Protected staging admin Organizations page loaded as the support super-admin. A staging-only test organization and organizer-admin invitation are prepared in the browser, awaiting action-time approval before granting access.
- 2026-09-17: Staging org-provision preflight omitted Access-Control-Allow-Origin for staging-admin, causing a browser fetch failure. Saved exact staging web/admin SITE_ORIGINS on staging only and verified the admin preflight now returns its origin. With user approval, created Race Pace Staging QA Organizer at 3% test commission and invited support.racepace+organizer-qa@gmail.com. Resend marks the app-generated invite Delivered; its branded HTML shows the logo. Redeeming its link landed on the invited admin's scoped staging Events page, without platform links. No production data changed. The remaining fetch-error crash and invite-dialog overflow were fixed locally; all 854 web tests and web typecheck pass, but these UI fixes are not deployed yet.
- 2026-09-17: Resend marked the triggered password-changed notice Delivered, with branded HTML visible in its preview. The generated invitation plain-text part still includes the logo URL and wraps the wordmark, so text formatting is not fully approved. iPhone Mirroring capture failed; real mobile Gmail inspection remains unverified.
- 2026-09-17: As the invited organizer admin, created and published `[TEST] Race Pace Staging Trail Run` (event `15a2ffd4-8856-42f2-87d2-3048770cdd0b`) with one 5 km category at ₱1,400 and five slots. With explicit user approval, published `[TEST] Staging QA Waiver — not valid for real events` and assigned it to the event. The public staging listing and detail page show the open event and registration link. The page mislabeled the 5 km fallback as vertical gain and the 05:00 flag-off as cut-off; corrected the label fallback locally, but this fix is not deployed. Runner sign-in expired before registration testing; user handoff requested. Production was not changed.
- 2026-09-17: User signed in again. The event blocked entry until the QA runner's Passport was complete. Saved synthetic Staging Runner details, reviewed the attached organizer waiver, and accepted/submitted the test entry with explicit user approval. Initial submission failed because `registrations-checkout` was missing from staging; deployed it after two local checkout tests passed. Retry returned HTTP 200 and opened payment for registration `b9f1d713-a442-46d6-b336-035fe88b5542`. Organizer console shows one pending registrant and the Passport fields. The event list remains at 0/5 paid slots while the pending hold exists; the database capacity trigger counts live pending entries, but UI capacity display needs review. Admin table/detail/export still use obsolete Bib labels. Production was not changed.
- 2026-09-17: Saved the existing local PayMongo `sk_test_` key to staging only without displaying it. Deployed payment-session, payment-verify and payments-webhook; all are active, with JWT verification disabled only for the signature-protected webhook. Created enabled test-mode PayMongo endpoint `hook_Rycf1WJREFL3PmRhtEwMCSsy` for staging and saved its returned signing secret to staging. Read-back lists both secret names; unsigned POST returned 401. Payment, webhook delivery, callback, ticket and reconciliation are not yet verified. Production was not changed.
- 2026-09-17: User completed PayMongo test-mode GCash checkout. Staging webhook invocation returned HTTP 200; registration and payment both read back as paid. Stored organizer waiver version and accepted timestamp are present, with `signed_in_self` method. The paid ticket visibly renders a QR, runner/category and shirt size M. Database ledger is ₱1,400 charge, ₱35 actual processor fee, ₱42 platform fee, ₱1,323 organizer net; admin Payments matches. Event capacity now displays 1/5. Admin registration BIB cell remains blank while the ticket displays B9F1D713. Ticket email, retry/failure/refund and race-day operation coverage remain. Production was not changed.
- 2026-09-17: Found staging issued its first ticket under the code's `dev-secret` signing fallback because TICKET_SIGNING_SECRET was absent. Added fresh staging-only signing and email bearer secrets plus exact public ticket URLs, deployed send-ticket-email and ticket-qr, and re-signed only this synthetic paid registration. QR endpoint returned PNG; unauthorized email invocation returned 401. A single manual test email was accepted and marked Delivered by Resend to the support test account. Its desktop preview visibly shows the Race Pace logo, staging banner, QR, ₱1,400 total and staging ticket link. Its generated plain text collapses adjacent table values, so formatting remains open. The automatic ticket-email path still needs a fresh paid checkout test; code must fail closed without a signing secret before production. No production changes.
- 2026-09-17: Changed the pass-on platform-fee display label to “Taxes and fees” in the runner payment page, PayMongo single-checkout line item, and planned group-checkout line item. The money calculation, commission terms, processor fee, absorb-mode display, and refund policy were not changed. Focused runner/group tests and runner typecheck passed. The first two labels are now deployed on staging; the group source remains local and its flow is unfinished. A label does not implement tax calculation or tax compliance; tax rules, receipts, refunds and reporting require a separate reviewed implementation before production.
- 2026-09-17: Committed the scoped single-checkout label change as `999228e` and pushed `feature/admin-ui-changes`. All 393 site tests passed. Published an isolated site snapshot without secret files to Vercel staging only; deployment `dpl_78fKB2PY6nFWbfnpfevAKeipQRgv` reached Ready at staging.racepace.com.ph. The snapshot includes current unfinished local site changes beyond that commit, so this is a staging test build, not a reproducible production release. Deployed kit-release and check-in functions to staging after 9 focused backend tests passed. The organizer released the synthetic Staging Runner kit and manually checked that runner in; admin history recorded the action and the runner ticket shows the collected kit. QR scanner and duplicate-scan behavior remain untested. Production was not changed.
- 2026-09-17: Reversed the synthetic runner's check-in through the organizer UI, then checked the runner back in. Both actions are retained in check-in history; the final roster is IN 1 / LEFT 0. The paid ticket's collected-kit status survives page reload. The runner-facing ticket does not show attendance, and actual QR scanning remains untested.
- 2026-09-17: The organizer payment page displayed ₱1,400 gross, ₱42 platform fee, ₱1,323 net and ₱0 refunded. Its registration page displayed one paid entry and ₱1,400 gross. Both CSV route suites passed 38 tests; two browser export clicks produced no captured download, so live CSV contents cannot yet be claimed. This is a test-tool or download-path gap, not proof that the export itself failed.
- 2026-09-17: Removed the publicly known `dev-secret` signing fallback from single-payment confirmation and check-in. Confirmation returns HTTP 503 before ledger correction if the configured secret is absent; check-in returns 503 rather than accepting a fallback-signed token. Focused missing-secret/check-in tests passed, as did 34 combined backend tests and 393 site tests. Verified the staging secret name exists, deployed payment-verify v4, payments-webhook v4 and check-in v2 to staging, and rechecked the synthetic runner successfully. The final roster is IN 1 / LEFT 0 with history. Scoped commit `97f431c` was pushed to `feature/admin-ui-changes`. Production was not changed.
- 2026-09-17: Reversed the synthetic staging check-in and loaded the signed QR value from its delivered Resend email into the check-in station's scanner input without printing the token. The first submission checked in Staging Runner as source Scanner; the second displayed an amber “already checked in” and did not add another current check-in. Read-only staging SQL confirmed exactly one current row. Malformed input and a forged signature produced distinct error messages. Camera access opened, but no physical QR was presented to it; optical decode remains unverified. The final roster remains IN 1 / LEFT 0. No production data changed.
- 2026-09-17: User chose to skip physical camera scanning. Created unpublished staging draft `[TEST] Wrong-Event QR Station` solely to test a second check-in station. Its 0-runner roster showed no history. The original event's valid signed ticket produced “Ticket belongs to another event” there; read-only SQL found one current check-in for the original event and zero for the draft. Returned the organizer console to the original event station. No production data changed.
- 2026-09-18: Replayed the original paid callback; it returned to the existing ticket, while SQL still showed one paid payment and one ticket token. Reused the staging draft as `[TEST] Staging Payment Failure Race` and created `[TEST] Staging Decline QA Race`, both ₱100 synthetic events. The runner authorized payment on the first; SQL records ₱100 gross, ₱2.50 processor fee, ₱3 platform fee and ₱94.50 organizer net. The runner selected Fail for GCash on the second. PayMongo left the browser on an expired-source page rather than redirecting to the merchant; SQL still shows pending registration/payment, no fee capture and no ticket. Manually returning to My Races showed a Complete payment link. A declined-card retry is pending. No production data changed.
- 2026-09-18: Added the existing Race Pace mark as the runner favicon and explicit icon/Apple touch metadata in both Next apps. Site and admin typechecks passed; 393 site and 854 admin tests passed. Scoped commit `99e451f` was pushed. Secret-free staging snapshots deployed to site `dpl_BWVbDKERaykxcMU3YRS9vpX8tTgu` and admin `dpl_H8zh1ocdQhLHar2WQRCdULiYpPdA`; both are Ready under their staging domains. The in-app browser showed the favicon metadata and loaded both 256×256 assets. Production was not deployed.
- 2026-09-18: Retried the pending ₱100 entry with PayMongo's generic declined test card. The user confirmed the decline. The browser remained on PayMongo's checkout instead of returning to Race Pace. The staging database still shows pending registration and payment, no ticket and zero fee capture. The visible merchant-return link led to Race Pace's “Payment cancelled” page with a retry link; Back to payment reopened the pending ₱100 payment page. This wording does not distinguish a decline from a voluntary cancellation. The refund policy still says “Race Pace service fee” after the checkout label changed to “Taxes and fees.” Both discrepancies remain open.
- 2026-09-18: Replaced the ambiguous callback heading with “Payment not completed” and removed the unsupported claim that no money was taken. The refund notice now refers to Race Pace fees using the “Taxes and fees” display label without asserting that tax calculation exists. Site typecheck and all 393 site tests passed. Deployed site `dpl_7xLbsz6iY5eXG9P3ZHEHkKwxj8yp` to staging; Vercel reports Ready and aliases staging.racepace.com.ph. The in-app browser verified both revised messages and the retry link. [PayMongo's hosted-checkout guide](https://docs.paymongo.com/docs/payment-channels-hosted-checkout-quick-start) describes merchant redirects after success or cancellation, not every decline; assess the GCash expired-source exit as a user-experience check. Production was untouched.
- 2026-09-18: The runner retried the same staging ₱100 registration with PayMongo test-mode GCash Authorize. PayMongo showed “GCash Payment Received!” and automatically returned to the Race Pace ticket. Read-only staging SQL confirms paid registration, one paid payment row, one ticket token, ₱100 gross, ₱2.50 processor fee, ₱3 platform fee and ₱94.50 organizer net. No duplicate payment row was created by the earlier failures. Production was untouched.
- 2026-09-18: Began a separate organizer refund test on the second ₱100 paid synthetic entry (`560979fb-9ea7-4fa4-98e0-007fc79dd6d0`). Admin detail showed Paid, but refund preview returned “Registration not found.” Staging SQL confirmed the registration/payment existed; the staging function list showed `admin-refund` was absent. Deployed only `admin-refund` with JWT verification enabled and import map; read-back shows ACTIVE v1. Reopening the dialog calculated ₱94.50 returned and ₱5.50 retained. No refund has been submitted. Production was untouched.
- 2026-09-18: User confirmed the prepared full refund in staging. Read-only SQL verified one succeeded test-mode provider refund and request for ₱94.50, both payment and registration refunded, and category capacity restored to 0/3. The runner ticket URL now says the pass is invalid. Organizer Registrations shows ₱0 gross and ₱94.50 refunds. The event settlement page nets to ₱0, and Payments aggregates to ₱1,417.50 organizer net across the two remaining paid entries. Live Payments and Registrations CSV downloads included the refund correctly. The original settlement CSV misleadingly showed the stored historical ₱94.50 under `net_to_org`, so a focused change exports ₱0 payable there and keeps ₱94.50 in `ledger_net_to_org`. Admin deployment `dpl_Eo2dGKkGYLvz6fXjrVb7P3tH5h5P` is Ready and aliased to staging-admin.racepace.com.ph. The re-downloaded CSV matches the page. Focused settlement tests (61), web tests (856), and web typecheck passed. This event has no payout statement, so payout accounting was not validated. The runner's My Races still counts a refunded entry under Upcoming. Production was untouched.
- 2026-09-18: User chose a database-only payout test instead of switching to the platform admin account. The first rollback-only staging transaction opened a ₱1,323 statement for the paid ₱1,400 event but unexpectedly allowed `payout_mark_paid` while the event date is in November. The console had shown this as Held. Added follow-up migration `20260917182452_guard_future_event_payouts.sql` to enforce the hold in the exposed RPC, retaining the existing exception for negative organizer recoveries. Local payout tests (41), group payout tests (2), function grant tests (8), focused admin payout tests (11), and admin typecheck passed. One separate group email test failed because the branded message now contains three images where it expects two; this payout change did not alter email code. Applied the migration to staging only and deployed admin `dpl_UKRe8JpigU7V7S5pzfk2gbbG1ww8`, Ready under staging-admin.racepace.com.ph. The early-statement warning now says payout remains locked until event completion. Staging rollback tests return `event_unfinished` with no payment stamp for the future event, and `paid` with one stamp when that event is temporarily set completed inside the transaction. The fully refunded event opens a ₱0 statement. An organizer-role RPC call to open a statement returned 42501. Final read-back shows the test event still open, zero persisted statements and zero payment stamps. No real transfer or production change occurred; the console payout path remains untested.
- 2026-09-18: Tested a partial refund on staging with a transaction that was rolled back. The existing ₱100 paid entry (`00c4ace8-94e0-437f-ab93-a247823e910b`) temporarily used a ₱20 organizer flat-fee policy. The live refund preview returned ₱74.50 to the runner and ₱25.50 retained overall. The refund database function marked the payment `partially_refunded`, kept the registration paid and the slot taken, and wrote one audit row. Its ledger reconciled exactly: ₱100 − ₱3 platform − ₱2.50 processor − ₱74.50 refund − ₱20 organizer net = ₱0. Read-back after rollback found the original full-refund policy, paid payment, ₱94.50 organizer net, zero refund and no partial-refund audit. No PayMongo refund or production change occurred. Local refund tests (26), admin financial tests (113), runner ticket tests (11) and admin typecheck passed. The real provider refund, browser views and exports are still unverified for this policy.
- 2026-09-18: Prepared a separate live-path partial-refund test on staging. Organizer admin created `[TEST] Staging Partial Refund QA Race` (`725f3de7-56aa-45a3-a963-e58d3d75ad14`) with one ₱100 category and three slots, attached the previously published test-only waiver, and opened registration. Staging organization `ab3e1d1b-82ef-4cb9-b785-3fa6464516ae` now has `flat_fee` refund policy with ₱20 retention. The runner review page explicitly discloses the organizer cancellation fee of up to ₱20 before submission. The registration is paused at the test-waiver acceptance step pending the user's approval; no new registration, payment or refund has occurred. Four additional local fee, commission and refund suites passed (58 tests). Restore or deliberately retain this test organization's flat-fee setting after the provider test.
- 2026-09-18: With specific user approval for the test waiver, registered synthetic Staging Runner in the flat-fee QA event. The user completed PayMongo test-mode GCash Authorize. The runner reached ticket `d2461c69-740b-4050-ad54-733efd7e07cb` with its QR. Staging SQL confirmed one paid ₱100 payment, ₱2.50 processor fee, ₱3 platform fee, ₱94.50 organizer net, zero refunded, and one of three category slots taken. The organizer refund preview offered ₱74.50 back with ₱25.50 retained, but incorrectly claimed the slot would reopen. Corrected the detail and modal copy to distinguish full and partial refunds. Admin staging deployment `dpl_9E7ioUaEejmtKbZdEwY96sWgGnoy` reached Ready; browser read-back on staging-admin showed the new copy and the same preview. The user has been asked to perform the final sandbox refund confirmation; no provider partial refund has been submitted yet. Local backend tests passed 632/632 with a temporary fake-provider function environment, then the original PayMongo test-key runner was restored. The test helper now fills required Passport fields and scopes two RLS assertions to their own records instead of the first 1,000 rows. Production was untouched.
- 2026-09-18: The user performed the final sandbox refund confirmation. Read-only staging SQL found a succeeded PayMongo refund request for ₱74.50, payment `partially_refunded`, registration still `paid`, organizer net ₱20, one slot still taken, and one partial-refund audit row. The runner ticket still displays its QR after reload. Admin Registrations shows ₱74.50 refunded and the detail history shows the event. Payments shows ₱25.50 retained gross, ₱3 platform fee, ₱20 organizer net and ₱74.50 refunded. The event settlement page balances ₱100 − ₱3 − ₱2.50 − ₱74.50 = ₱20. The in-app browser blocked the fresh settlement export URL, so the actual partial-refund CSV download remains unverified; the CSV formatter has a matching unit case. The detail panel incorrectly said no payment existed after partial refund, and aggregate cards called refund-adjusted receipts “Gross.” Both were corrected and verified live in admin deployment `dpl_32SPZP8CjPDiEf4VkiDhAnQrMgBV`. The ticket does not itself show the refund adjustment. Production was untouched.
- 2026-09-18: Resend marked the automatically generated flat-fee QA ticket email Delivered. Its desktop preview loaded the Race Pace logo and QR, and contained the correct staging ticket link. Its generated plain text ran Event, Category, Date and Total together. Added explicit line-separated single/group ticket text and passed it through Resend and SMTP. Focused email tests passed 22/22; staging `send-ticket-email` v2 and `group-ticket-delivery` v1 are Active. A fresh delivered email, real mobile client and group flow must verify this revision. The group feature remains gated. Production was untouched.
- 2026-09-18: A rollback-only staging transaction proved two-organization read/write isolation for private drafts, registrations and payments without leaving a second org or user. Corrected staging's GCash rate card from 1.5% to 2.5% with follow-up migration `20260917193346_correct_gcash_processor_rate.sql`; four earlier sandbox captures and PayMongo's published GCash price motivated the change. Local rate and fee tests passed. A fifth synthetic zero-commission/pass-on registration charged ₱102.57 for a ₱100 entry through GCash and returned to a signed ticket. Staging SQL confirmed one paid payment with ₱2.56 actual processor fee, ₱0 platform fee and ₱100.01 organizer net. The disclosed processing line was ₱2.57; document the one-cent mismatch and move to PayMongo `/v2/checkout_sessions` with provider-managed `pass_on_fees` before enabling pass-on in production. Closed the test event and restored the QA organizer's prior 3% commission and absorb mode. Production was untouched.
- 2026-09-18: The user clarified the pilot pricing rule during review of a new synthetic ₱100 event: the runner pays exactly ₱100, with Race Pace and actual PayMongo fees deducted from that gross. The staging QA organizer was restored to 3% commission and `absorb`. No waiver was accepted and no registration or payment was submitted for the new event. It was renamed `[TEST] Fixed-Price Fee QA Race`; admin list and runner page show the new name. The runner review page shows ₱100 total and explains that ₱3 in Taxes and fees is included, with the actual PayMongo fee deducted after capture. The staging site deployment `dpl_GFdJrnMyNdqtnMLGUfncZGHy7APq` is Ready and aliased to staging.racepace.com.ph; browser read-back verified the text. Staging functions `registrations-checkout` v5, `payment-session` v6, `payment-verify` v6 and `payments-webhook` were deployed. One PayMongo URL is reused on repeated payment taps, and absorb captures above the agreed price fail closed. Local site tests passed 391/391, provider/group focused tests 71/71, two focused backend checks and site typecheck passed. Read-only staging SQL confirmed the organizer terms and zero registrations for the event. A sandbox capture, session expiry and durable reconciliation remain before production. No production change occurred.
- 2026-09-18: The user separately approved this synthetic event's test-only waiver and registration after the fixed-price correction. Runner browser reached the payment page for registration `ea72bd25-1e14-44b7-a91b-dda9dd8c5b97`, showing ₱100 total due, ₱0 added at checkout and ₱3 included in the price. Read-only staging SQL confirmed pending registration/payment, a recorded waiver version, 10,000-centavo checkout amount, 300-centavo frozen Race Pace fee, `absorb` mode, and a PayMongo `cs_` reference. The final PayMongo test payment is handed to the user; no capture or ticket is claimed yet. Two new focused UI assertions passed 29/29 after an initial test-only duplicate-text selector was corrected. Production was untouched.
- 2026-09-18: The user completed test-mode GCash Authorize for the fixed-price ₱100 reservation. The runner reached signed QR ticket `ea72bd25-1e14-44b7-a91b-dda9dd8c5b97`. Staging SQL confirmed paid registration/payment, one payment row, one ticket and one slot. The PayMongo paid webhook reported 10,000-centavo gross, 250-centavo actual processing fee and 9,750-centavo processor net in test mode. The stored ledger recorded ₱100 gross, ₱3 Race Pace fee, ₱2.50 processing and ₱94.50 organizer net. Organizer Payments shows ₱100 gross/₱3 fee/₱94.50 net; event Settlement shows the full ₱100 − ₱3 − ₱2.50 = ₱94.50 waterfall. Its CSV link is blocked by the Codex in-app browser (`ERR_BLOCKED_BY_CLIENT`), so downloaded bytes are not claimed. Maya/card method tests, stale-session expiry and durable duplicate/late-capture reconciliation remain. Production was untouched.

- 2026-09-18: Closed part of the uncertain checkout-creation gap. New single payments freeze the exact server-built PayMongo request before the external call. PayMongo receives a stable `checkout:<registration_id>` idempotency key; payment-session can retry a missing session with the same payload inside a conservative 23-hour window. A concurrent duplicate routes to the pay page instead of a dead-end `payment_creation_pending` response. Local migration, 12 focused provider/grant tests, SQL privilege and immutable-request checks passed. Applied staging migration `20260917210010`; registrations-checkout v6 and payment-session v7 are Active. No new sandbox checkout was created for this recovery path, so its live lost-response behavior remains unverified. PayMongo session expiry and a durable capture inbox remain P1 launch blockers. Production was untouched.

- 2026-09-18: Added a local-only service-role capture inbox and payout hold for single PayMongo payments. Confirmation records each paid payment ID before fulfillment, including multiple paid IDs in one session. Replays remain idempotent; extra IDs and amount mismatches require review and hold organizer payout. Four focused files passed 40 tests, site typecheck passed, and `git diff --check` passed. The migration is applied only to the local database. It has not been deployed or tested on staging or production. Explicit provider-session expiry, unmatched capture handling, reconciliation alerts and live payout verification remain open.

- 2026-09-18: Enabled Vercel sign-in protection for both staging custom domains and verified unauthenticated HTTP 302 redirects to Vercel sign-in. This is the active crawler boundary; robots directives alone cannot guarantee exclusion. Isolated production gate code passed local site/admin typechecks, 2 launch-gate tests, 374 site tests and 815 admin tests. The first-draft page was built on a generated Vercel URL without assigning the production custom domains. The user requested better visual options, so production promotion is paused. Three responsive designs, desktop/mobile screenshots and a selectable local preview now live in `docs/previews/coming-soon-options/`. No production custom domain or database was changed by this design step.

- 2026-09-18: The user rejected all three first-round concepts and the use of an existing race photograph. Removed their preview files and replaced them with Horizon, The Route and Ascent. All three use white-led Race Pace greens and the transparent logo without a backing panel. Horizon uses a new fictional mountain illustration generated for this concept; the other two use custom SVG mountain/route graphics. The selectable local preview includes subtle pointer parallax with a reduced-motion fallback. Desktop and mobile screenshots were reviewed. Production custom domains and databases remain unchanged.

- 2026-09-18: Updated Horizon from browser feedback: removed the short line before `COMING SOON` and increased its label size to 24px on desktop and 18px on mobile. Regenerated and inspected both screenshots. The concept remains a local preview; production custom domains were not changed.

- 2026-09-18: Checked the coming-soon preview at 320, 375, 390, 430 and 768 pixel widths. All three concepts had no horizontal page overflow, clipped headings or copy/footer overlap; links retained a 44px hit area. At 320px, shortened Horizon's organizer link to avoid an awkward wrap, shortened The Route's link, and reserved space below the preview so its fixed design switcher does not cover the footer. Pointer parallax now runs only on fine-pointer devices and still respects reduced motion. Refreshed Horizon 320px, 390px and 768px screenshots. Production remains unchanged.

- 2026-09-18: Added visible depth parallax to Horizon's fictional mountain illustration. A static base image sits beneath far, middle and foreground masks that shift by 12, 25 and 42px at pointer extremes. Fixed the preview switcher's URL parsing error that had stopped its prior pointer script. Browser QA verified distinct left/right layer transforms, successful concept switching, no page errors, and no horizontal overflow at 320, 390 and 768px. Touch devices and reduced-motion preference render only the static base. Refreshed the screenshots; production remains unchanged.

- 2026-09-18: Revised Horizon's organizer contact from browser feedback. Removed the awkward arrow and divided underline; the prompt now sits beside a forest-green `Get in touch` button, wrapping cleanly on 320px screens. The `mailto:support.racepace@gmail.com` target is unchanged. Desktop, 390px and 320px screenshots were regenerated and inspected. Browser checks confirmed a 44px button target, no horizontal overflow and no page errors. Production remains unchanged.

- 2026-09-18: The user found ghosted/doubled mountain edges in the layered hover treatment. Replaced the stacked masked copies with one illustrated landscape that translates up to 28px per axis and returns to center on pointer exit. Desktop left/right captures show crisp ridges; browser QA confirmed exactly one image layer, distinct motion at pointer extremes, no script errors, static touch/reduced-motion behavior and no horizontal overflow at 320, 390 and 768px. The preview remains local; production remains unchanged.

- 2026-09-18: Deployed the approved Horizon Coming Soon gate to production web and admin custom domains. The latest site deployment is `dpl_8ZU9EsGG5MAo2jmUGubn2MaaD6Rq`; admin is `dpl_HBXZ3c3HJDZzPC9M3FAv2aUnfjvZ`. Production routes including registration, payment, ticket, login and admin operations redirect to the holding page; POST requests return 503. The apex redirects to `www`. The live page loads the transparent logo and fictional mountain illustration. After user mobile feedback, removed the hard landscape edge with a fade and set explicit equal 24px gutters. Live browser measurements at narrow width showed 24px on both sides, no horizontal overflow and a loaded logo. The source remains uncommitted in isolated `codex/coming-soon` worktree, so integration into reviewed Git history is required before any normal CI/CD deployment. No production database data was changed.

- 2026-09-18: Rechecked both staging Vercel projects after the production deployments. Each retains Standard Protection (`prod_deployment_urls_and_all_previews`), and unauthenticated requests to both staging domains, internal routes and `robots.txt` return HTTP 302 to Vercel sign-in with `no-store`. This blocks public crawlers from page content. A spot search for both staging hostnames returned no results; that does not prove a future or historical URL-only listing is impossible. Review Search Console if one appears.

- 2026-09-18: Audited the proposed organizer email addresses. A recursive DNS lookup found no MX record for the root racepace.com.ph domain, so inquiries@racepace.com.ph and onboarding@racepace.com.ph must not replace the working Gmail contact yet. Resend supports inbound email through a receiving domain and webhook/API, but the recommended human reply workflow is one branded mailbox with those two aliases; configure the chosen provider's root-domain MX and authentication records in GoDaddy, then verify external inbound delivery and replies from both aliases. Leave the verified notify.racepace.com.ph transactional sending domain unchanged. No DNS, provider, or public CTA change was made.

- 2026-09-18: Investigated the Google Workspace postal-code rejection in the user's in-app browser. The existing Gmail-account upgrade checkout rejected a four-digit Philippine postal code and linked its lookup to Singapore Post; the checkout did not display its billing country. Started Google's separate new-account flow from its Philippines business page. Its first form explicitly showed Region: Philippines; entered Race Pace as the business name and selected one user. The flow now awaits the account owner's first name, last name and current email. No checkout, billing profile, domain verification, DNS or public CTA change was completed.

- 2026-09-18: Continued the Philippines new-account flow using the owner's entered contact name and the designated Race Pace Gmail contact. Google accepted the existing racepace.com.ph domain and reached “Create a username.” The owner must choose and enter the new credentials and personally accept the Workspace agreement before the billing country can be verified on the next step. No account agreement, checkout, billing profile, DNS or public CTA change was completed by the assistant.

- 2026-09-18: The owner created team@racepace.com.ph, accepted Google's signup terms and completed phone verification. The new domain-based checkout selected Business Starter on the monthly plan. Google Payments saved a Race Pace organization profile explicitly marked Philippines, which resolves the former Singapore postal-format mismatch. The attempted card was rejected with Google's prepaid-card restriction; no subscription checkout or charge was completed. The owner must use an accepted payment method and perform the final checkout. Domain ownership, mail records, aliases, inbound/outbound tests and the public CTA remain unchanged.

- 2026-09-18: The owner deferred Google Workspace and branded organizer aliases. Keep support.racepace@gmail.com as the working public contact while using the existing Resend transactional sender. Do not change the Coming Soon link or root-domain mail DNS until the owner resumes the mailbox setup. Resume the pending release and staging payment-safety work instead.

- 2026-09-18: Copied the approved production holding-page source from its isolated worktree into the current feature working tree without replacing unrelated edits. Site and admin typechecks passed, as did the two focused launch-gate tests. Applied the provider-confirmed expiry migration to local Supabase only and added three rollback-only database tests. They verify an expired reservation remains pending until PayMongo confirms expiry, a mismatched session cannot close it, an observed capture blocks expiry, and a missing session stays pending when its event closes. The worker now re-reads PayMongo after an expiry request before changing local state. Capture/grant tests passed 15/15 and registration/fee/grant regression tests passed 57/57. The local Edge runtime container is absent despite being enabled in the config, so the worker HTTP smoke check could not run. No staging migration, worker schedule, commit, push or production deployment occurred in this step.

- 2026-09-18: Restored the local Edge runtime without deleting its database volume. The expiry worker returned 401 without a credential and 405 to a GET, confirming its handler is running. Corrected CI to wait for that handler and share its webhook/ticket test secrets. Kept malformed paid PayMongo capture metadata in the review inbox so payout stays held, and made the signed-capture integration fixtures use distinct payment IDs with local cleanup. Site tests passed 392/392, admin 858/858, backend 656/656 and both app typechecks. The approved production holding-page source was committed locally as `5511859`. A staging-only CLI dry run from the isolated workdir linked to `pepbmqomiailnnvvwupz` listed exactly two pending migrations: `20260917211002` and `20260917212328`. No staging database change, worker schedule, push or new production deployment occurred yet.

- 2026-09-18: Tested PayMongo sandbox checkout creation with identical request bodies and identical `Idempotency-Key` headers. Both v1 and v2 returned HTTP 200 with distinct `cs_` IDs on retry, contrary to PayMongo's general idempotency reference. All four unpaid sessions were expired; GET confirmed `expired` and zero payments. A separate expired sandbox session was bound to a synthetic staging reservation. The scheduled worker's first nonempty run called provider expiry, re-read `expired`, marked the registration `expired` and payment `failed`, and left no ticket, capture, occupied paid slot or remaining candidate. The staging fixture, including its user and organization, was deleted and read-back confirmed no residual rows. The account key used for the probe was accepted by the staging worker. Local fixes now fail closed when checkout creation has no bound provider session, and recheck available paid sessions for extra captures; deployment and live verification remain. Site 406, admin 871, backend 689 tests and both typechecks passed before the final UI copy and no-retry changes; focused tests for those changes passed afterward. Production was untouched.

- 2026-09-18: Merged PRs #25–27 into staging after their required checks passed. The exact-merge CI run for #27 (`35307809883`) passed, and the new staging site deployment reached Ready. A signed-in runner reload showed Upcoming · 5 and Inactive · 1, removing the fully refunded future race from Upcoming. The in-app browser session could not activate the Inactive tab, so the tab contents remain covered by focused tests rather than live click-through. Rechecked that a production event route redirects to Coming Soon, a production POST returns 503, and unauthenticated staging `robots.txt` redirects to Vercel sign-in. No production app or database promotion occurred.

- 2026-09-18: Merged payment-safety PR #29 into `staging` at `f5a449c` after both required PR checks and Vercel previews passed. The exact-merge CI run `35317936037` also passed. Staging site deployment `dpl_8aBgGUirVvEEFUwczCwHba2jEnS3` and admin deployment `dpl_F4kmjdNoq1253Eqv9EW2oqmSFAzy` are Ready on their custom staging aliases at that exact commit. Applied only migration `20260918115000` through an isolated CLI workdir linked to staging; the subsequent dry run listed no pending staging migration. The CLI reported a pg-delta cache warning after applying it, so read-only SQL separately confirmed the migration row, trigger and function. Deployed `payment-session` and `payment-verify` v10 with the project's import map; both are Active with JWT verification and returned 401 to unauthenticated POSTs. A rollback-only staging test recorded two missing-session attempts, produced one platform `payment_review` notification, kept registration and payment pending, and left zero QA users. Public requests to staging `robots.txt` still redirect to Vercel authentication; a production registration route still redirects to Coming Soon. Authenticated checkout refusal, a controlled late/duplicate capture, capture-alert delivery and operator resolution remain unverified. Production was not changed.

- 2026-09-18: Drafted `docs/plans/2026-09-18-unbound-paymongo-review.md` under the PIV planning workflow. Code review found no platform notification inbox or admin action for a `missing_session_ref` hold, although the worker and trigger record it. The plan starts with a platform-only queue and auditable provider evidence; it deliberately forbids slot release until PayMongo can establish a complete session history for the registration. No operator code or production change occurred in this planning step.

- 2026-09-18: Implemented the first read-only operator slice locally: `platform_unbound_checkout_reviews()` lists pending single-runner PayMongo payments with no saved provider session, even before an expiry attempt. It exposes a limited projection only to platform super admins. The admin page shows the held registration reference, event, amount, deadline, attempt count and capture count, with no release/retry/bind control. Focused database tests denied organizer, runner and anonymous calls; the fixture rolled back and left its hold unchanged. Full local admin 877/877 and backend 692/692 tests, admin typecheck and diff checks passed. The migration and UI await PR review, staging deployment and browser role verification. Production was untouched.

- 2026-09-18: PR #32 passed both required checks and Vercel previews, then merged into staging as `ad4dd08`. Its exact-merge CI run `35321138949` also passed. Applied only migration `20260918120000` through the isolated staging CLI link; its post-apply cache warning did not affect the database, as SQL confirmed the migration row, RPC and grants, and a fresh dry run listed no pending migrations. Staging site `dpl_3Jj5r1E6PvDXipskVJsQBqh1dNsr` and admin `dpl_5HBea4tsayQan27o5thnkSAJAJ8z` are Ready and aliased at that commit. In the signed-in organizer browser, `/checkout-reviews` returned 404 and its nav item was absent. A rollback-only staging fixture produced one platform queue row with the expected amount, denied the organizer role, and left no QA organization. The real queue baseline was empty. Then a committed synthetic unbound hold was picked up by the five-minute scheduled worker: one `missing_session_ref` attempt and two platform notification rows appeared, while registration/payment remained pending, captures remained zero and paid slots taken remained zero. The exact QA notification, attempt, payment, registration, category, event, organization and user rows were deleted in one transaction; read-back found zero residual QA rows. No provider payment, release, or production change occurred.

- 2026-09-18: Repeated the requested simple staging journey on exact `staging` revision `e423379`. The organizer published `[TEST] Release Checkout QA Race` (`0cc509b5-528a-4055-8e93-5035065d959b`) with one ₱100, three-slot 5K category and attached the published test-only waiver. After explicit waiver approval, Staging Runner registered as `298b2094-5e97-4d07-914d-05702fff9bc7`. The user completed PayMongo test-mode GCash. The runner ticket displayed a QR; SQL showed one paid registration, one settled capture (`livemode=false`, ₱100 gross, ₱2.50 actual provider fee, ₱97.50 provider net, delivery count two), one occupied slot and one ticket. The ledger and admin Payments agreed on ₱3 Race Pace fee and ₱94.50 initial organizer net. The organizer's existing ₱20 cancellation fee produced a disclosed ₱74.50 refund preview. The user confirmed the sandbox refund; the provider refund request became `succeeded`, the payment `partially_refunded`, and the ledger retained ₱20 organizer net and one occupied slot. Refreshed Registrations showed ₱25.50 retained gross and ₱74.50 refunded; Payments and Settlement agreed with ₱100 − ₱3 − ₱2.50 − ₱74.50 = ₱20. The in-app browser did not yield downloaded CSV bytes from Payments or Settlement links, so these exports remain unverified for this entry. Production was untouched. Next: complete production backend/deployment preflight, keep Coming Soon closed, and use an internal protected path for test-mode production QA.

- 2026-09-18: Began the authorized prelaunch production cutover after that staging walkthrough. An isolated `codex/production-cutover-20260918` worktree at `e423379` linked to the production project and dry-run listed exactly 38 pending migrations. The Supabase dashboard showed seven scheduled backups, latest 17 Sep 2026 19:26 UTC; it warned that Storage objects are excluded. Production Auth and Storage objects predated that backup. Applied the 38 migrations through the production-linked CLI without seeding or resetting. The CLI exited successfully after a nonfatal pg-delta catalog-cache warning. Independent SQL confirmed 136 migration versions through `20260918140000`, and a fresh dry run said the remote database is up to date. Counts remained one organization, two events, one registration, one payment and five Auth users; five Passport rows were provisioned. At that checkpoint, no production function, Vercel deployment, PayMongo secret, webhook, worker or test transaction had changed yet. Coming Soon remains closed.
