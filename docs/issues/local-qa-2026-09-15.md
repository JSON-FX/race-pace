# Local QA restoration findings

No GitHub issue was supplied. Investigation and implementation stay local.

## Historical processor-rate test

Reproduced: 401/402 backend tests passed; historical rate assertion returned no row. The test queries 2026-08-15, while the local rate was seeded on 2026-08-18. The migration uses `effective_from DEFAULT now()`, so the query correctly returns no rate. Confidence high; test fixture defect, not evidence of a payment defect.

Fix `supabase/tests/processor-rates.test.ts`: use a unique provider and two explicitly dated rate intervals. Assert setup and RPC errors. Delete only the isolated provider rows. Verify dates before, at and after the interval boundary; run the backend suite.

## Local image configuration

Reproduced in Computer: homepage crashes with next/image unconfigured hostname. Twenty local sample events contain hosted Supabase URLs, but both apps only allow the currently configured Supabase host. Local storage contains no objects. Also the current config uses URL.host (including port) as hostname and hardcodes HTTPS, so local HTTP storage cannot match.

Fix both next.config.ts files to use the configured URL's protocol, hostname and port. Local development can display storage images without the server image optimizer because 127.0.0.1 inside Docker is not the host. Keep production optimization unchanged. Clear only the stale hosted image references in local sample events after backup; no hosted writes. Validation: app typechecks and local homepage/browser signup.

## Docker session cookie mismatch

Computer signup and confirmed login both returned to a homepage showing Sign in. Server/middleware use host.docker.internal while browser uses 127.0.0.1. Installed supabase-js derives `sb-${hostname.split('.')[0]}-auth-token`; SSR accepts cookieOptions.name. Thus server reads sb-host-auth-token while browser writes sb-127-auth-token. Confidence high.

Fix both apps' server.ts and middleware.ts to set cookieOptions.name from NEXT_PUBLIC_SUPABASE_URL, retaining the browser's default and the hosted cookie name. Validate app typechecks/tests and a protected page after login. No auth gate changes.

## Slot-release grant drift follow-up

2026-09-15: CLI projects list succeeds but excludes active project whaqarofxdlzxrelbcrq. Linked migration listing returns 403. Connector SQL is also denied; Computer control times out. Hosted verification remains blocked by access.

The earlier hosted read proved decrement_slot grants anon/authenticated EXECUTE; local denies both. Original migration 20260722100000 revokes only PUBLIC and grants service_role. Named grants survive that revoke. Added follow-up 20260914203631_restrict_slot_release_rpc.sql explicitly revoking all three client roles while retaining service_role. Applied locally only. Added capacity-integrity regression test: denied anonymous/runner calls leave count unchanged, service role releases one slot.

Validation: 32 tests passed across function-grants, admin-roles, and org-management. Remaining table/column/function drift needs a full hosted catalog comparison; this one fix does not establish parity. Hosted migration is pending, and browser end-to-end testing has not resumed.

## Refund and payout walkthrough — 2026-09-15

- PASS (Computer): North admin refunded QA Mail Flow Runner (4a2468ee-babd-45b5-a156-16eeef768d07). Stored refund PHP 955, slot count 3 to 2, paid_at preserved and refunded_at populated. Settlement PHP 3000 - 90 - 45 - 955 = 1910.
- FAIL: refund drawer, confirmation and success toast advertise PHP 1000, but provider path and ledger use PHP 955. RefundModal derives its amount from total_amount instead of refund policy/net_to_org; success ignores pending/already result. Correct amount preview and outcome messaging before launch.
- PASS (Computer): platform login; opened North statement 6abc5fbd-fd59-4b96-b46b-66bf864c93b1 for PHP 1910. Payment action locked while event unfinished.
- QA fixture setup: marked only North event 6cf4de5f-decf-4877-8bd0-7dd9abf7859f completed in local DB. Dates unchanged. It remains completed for these payout tests.
- PASS (Computer): recorded simulated payout reference QA-SIMULATED-20260915-001. No actual bank transfer.
- PASS (authenticated API): refunded QA Snapshot Runner after payout (0cd4949d-affb-47ef-95a8-6081cc51d4e2).
- PASS (Computer): next statement 3b0f8a07-d870-40b4-ba11-9e453855e0a6 showed PHP -955 owed back; recorded simulated recovery QA-SIMULATED-RECOVERY-20260915-001. No actual transfer.
- PASS (authenticated RPC/readback): subsequent statement 87ed5a86-ba10-4528-b3c2-e8449efbbded has zero net and zero refunds. Reopening concurrently/existing open rejected with 23505. No duplicate earnings or clawback in this sequential scenario.
- PASS: 32 focused tests across payout-statements, payout-statements-v2, refund-net-to-org and refund-policy.
- OPEN: refund UI amount/pending feedback; misleading already banked and exact settlement copy; real provider pending/failure and fee confirmation; flat-fee/pass-on browser journeys; refund after statement opening before payment; payout exports. No hosted deployment or production readiness claim.
