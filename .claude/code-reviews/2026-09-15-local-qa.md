# Local QA setup review

Scope: this task's Next image configs, local Supabase configuration, processor-rate fixture and QA documents. Pre-existing CLAUDE.md and admin layout changes are excluded.

Reviewed: local SMTP secrets are environment references; real values remain ignored. The app API/keys are local. Image protocol/host/port come from the configured endpoint; optimization is bypassed only for HTTP development. Rate fixtures are isolated by unique provider and cleaned in finally. No applied migrations or hosted data were changed in this setup phase.

Validation: backend 402/402 passed; both Next app typechecks passed; Computer verified homepage rendering, local signup, Mailtrap capture and email confirmation. No additional implementation findings. Runtime permission drift, signup UX/callback, and ticket delivery remain recorded in the checklist, so this is not production signoff.

Additional review: four server/middleware clients now explicitly use the public endpoint-derived cookie name. It matches the installed Supabase default, preserving hosted sessions while fixing Docker hostname mismatch. Both app suites passed (322 + 739); both typechecks passed. Computer verified signed-in protected My Races access.

Slot-release follow-up reviewed: only named-role EXECUTE grants are revoked, service_role remains granted, and the SQL function/policies are unchanged. New test checks capacity remains unchanged for denied roles and decreases for service_role. Relevant suites: 32 passed. Applied locally; hosted deployment and comprehensive permission parity unverified.

Permission normalization reviewed after scoped-column correction: only unintended client table privileges removed, policy-helper access restored, original column allowlist explicitly regranted. 403 backend tests passed; hosted client grants match local after application; 89 migrations aligned. No client permission findings remain in the compared catalog.

## Sandbox handoff follow-up

Reviewed FakePaymentProvider return URL fix and regression tests. Encoding retains nested callback query parameters; provider reference unchanged; real PayMongo implementation and guard unchanged. Unit tests plus backend handoff integration pass (30 cases). Browser retry reached ticket and reconciled PHP 1,000 = PHP 30 + PHP 15 + PHP 955. Existing fake-checkout return-target HTML interpolation remains a separate unresolved hardening concern. Full name loss and inaccurate ticket-mail success copy were recorded in the two-organization walkthrough. No deployment performed.

## Registration identity review

New registration snapshots include full_name; optional save-back includes core identity. Ticket and settlement use a shared snapshot-first resolver with legacy profile fallback. Two admin views preserve security_invoker; checkin_roster preserves its authorization predicate and explicit grants. Focused RLS/grant tests pass. Browser and SQL prove a fresh paid runner with null profile names is now named on all four operational screens. No historic row updates. Remaining limitations are documented in issue-registration-identity.md. Migration is local only; no hosted release claim.

## Mailtrap ticket transport

Reviewed explicit provider selection, sandbox-only hostname, required TLS, pinned dependency, transport closure and no live fallback. Existing protected endpoint and payment confirmation logic unchanged. Ten tests pass for template escaping, sandbox success/failure and default Resend behavior. Local SMTP integration and automatic payment-triggered capture verified in Mailtrap. Secrets only in ignored env. Browser confirms corrected ticket copy and email link. No hosted deployment.

## Deferred export context

Reviewed request-scoped client capture and optional query injection. Both routes retain ordinary authenticated clients and existing org gates; no role elevation. Registration stream disables other per-batch email/addon reads and passes its captured client to the separate email lookup. North/South live endpoint tests confirm isolation and totals. Full admin tests and typecheck pass; browser download remains separately blocked by Brave. Settlement timestamp and processing-column findings retained for next stage.

## Financial reporting fixes verified locally

- FIXED: migration 20260915115501 records payment confirmation time atomically. Existing dates are backfilled only from matching system paid audit entries; unknown legacy dates remain blank.
- FIXED: settlement reads confirmation time and validated refund time. Payment CSV includes recorded processing fee/source and distinct checkout/confirmation timestamps.
- PASS: 743 admin tests and typecheck; final export test rerun 13 passed; reporting and function-grant checks 9 passed; git diff --check clean.
- PASS: authenticated HTTP payment export returns North's three rows and South's header only. Each North row shows PHP 1000 gross, 30 commission, 15 predicted processing, 955 net. Confirmation timestamps differ from checkout timestamps.
- PASS: direct database test confirms payment replay preserves paid_at and refund preserves paid_at while recording refunded_at. This is not a browser refund/payout pass.
- OPEN: browser download remains unverified. Full refund/payout browser reconciliation and PayMongo test mode remain pending. Settlement projection says already banked and exact despite predicted processing fees; review this copy.
- LOCAL ONLY: timestamp migration and code have not been released to hosted Supabase or Vercel. Production readiness remains NO.

## Refund feedback review

Reviewed modified endpoint, shared refund helper, actions, modal, drawer and new tests. Existing per-organization authorization remains ahead of preview and mutation. Preview never calls provider or writes. Expected amount is a guard only; authoritative amount is recalculated server-side. Existing provider-first sequence preserved. Pending/already messaging no longer asserts success. No dependency or migration changes.

Blocking finding outside the feedback implementation: stale payout aggregate can settle after an intervening refund. Detailed reproduction in docs/issues/issue-payout-stale-statement.md. Overall production readiness FAIL despite feedback checks passing.

## Stale payout guard review

Reviewed forward migration against dumped live aggregate/stamp functions and reviewed query/action/dialog changes. No accounting formula changed. Snapshot comparison catches membership as well as amount changes; revision catches another admin refreshing while a dialog remains open. Locks include row-locking writers to avoid a compare/stamp gap. Old open snapshots NULL fail closed. New internal helper is not browser-executable. Authorization and grant checks pass. Paid history refresh is refused. Table-wide contention remains a deployment performance limitation; no actual bank transfer is performed by this code.
